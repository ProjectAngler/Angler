conky.config = {
    alignment = 'top_right',
    background = false,
    update_interval = 1.0,
    total_run_times = 0,
    double_buffer = true,
    no_buffers = true,
    cpu_avg_samples = 2,
    net_avg_samples = 2,
    use_xft = true,
    font = 'DejaVu Sans Mono:size=22',
    xftalpha = 1.0,
    own_window = true,
    own_window_class = 'AnglerMonitor',
    own_window_type = 'normal',
    own_window_argb_visual = true,
    own_window_argb_value = 205,
    own_window_colour = '10151c',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    minimum_width = 1756,
    maximum_width = 1756,
    minimum_height = 940,
    gap_x = -16,
    gap_y = 28,
    border_inner_margin = 30,
    draw_shades = false,
    draw_outline = false,
    draw_borders = true,
    draw_graph_borders = false,
    default_color = 'd7e3f4',
    color1 = '65d1ff',
    color2 = '79e6a7',
    color3 = 'ffcc66',
    lua_load = '/home/becca/.config/conky/gpu_graphs.lua',
    lua_draw_hook_post = 'draw_gpu_graphs',
};

conky.text = [[
${color1}${font DejaVu Sans Mono:bold:size=32}ANGLER WORKSTATION${font}${color}${alignr}${color 8294aa}${nodename}  ${kernel}${color}

${color1}CPU${color} ${cpu}%  ${freq_g} GHz${goto 430}Load ${loadavg}${goto 1080}Processes ${processes} / ${running_processes} running
${color2}RAM${color} ${mem} / ${memmax} (${memperc}%)${goto 890}${color1}DISK${color} ${fs_used /} / ${fs_size /} (${fs_used_perc /}%)
${membar 18,830}${goto 890}${fs_bar 18,830 /}

${voffset 590}
${color 8294aa}GPU history: 30 seconds / 1 second samples${alignr}Updated ${time %H:%M:%S}${color}
]];
