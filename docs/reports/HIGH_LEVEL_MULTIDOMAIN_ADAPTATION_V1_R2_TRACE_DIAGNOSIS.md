# High-level multidomain adaptation V1-R2 trace diagnosis

Date: 2026-09-01

Diagnostic identity:
`angler.high-level-multidomain.v1-r2-posthoc-trace-diagnosis.v1`

Disposition: integrity `PASS`; bounded trace questions answered; predecessor
scientific classification remains `NOT_SUPPORTED`

## Immutable artifacts

- Create-once diagnostic result:
  `/opt/angler/results/high-level-multidomain-v1-r2-trace-diagnosis.json`
  (`4906c3a77e851012ec2cc12172016ef409ad68c9f1efa4455bb1d570b19eb693`,
  100,017 bytes, mode `0600`, one link)
- Frozen diagnostic runner:
  `experiments/runners/high_level_multidomain_v1_r2_trace_diagnostic.py`
  (`79068362cb5f30c43e70b6f8d73167eaf2e6c95850de045644f900aea006eeb7`)
- Frozen diagnostic unit suite:
  `tests/unit/experiments/test_high_level_multidomain_v1_r2_trace_diagnostic.py`
  (`39fe6f2b67bfa119a64830d6a877c9b48986c810029e3a437b4f4313dee12c41`)
- Accepted pre-run diagnostic leaf:
  `docs/blueprints/branches/science/work/ANG-WORK-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-R2-EVIDENCE-TRACE-DIAGNOSIS-001.md`
  (`eedc848394a088e347db68db23f24fabab409315a0775747597edc76abd5f960`)

The result records 15 identical before/after input witnesses: seven preserved
repository inputs at their exact mode `0664` and eight sealed result/state
inputs at mode `0600`. Every hash, owner, mode, link count, size, inode
identity, and modification-time check passed. No SQLite sidecar, temporary
output, model, GPU, Cognee worker, network access, or subprocess was present.

The final CUDA-hidden synthetic suite passed 31 tests plus 16 parameterized
subtests. Two independent pre-run reviews accepted the exact runner and test
bytes. Two independent post-run reviews accepted the durable result, its
redaction boundary, and a fresh in-memory reconstruction over 1,280 captured
forbidden raw values. The reconstruction matched every scientific,
provenance, matrix, question, and input field exactly. Its process-local wall,
peak-RSS, and derived output-size observations were normalized only for that
cross-process comparison; the durable values were independently checked
against the actual file and frozen ceilings.

## Pre-publication refusals retained as construction evidence

Three fail-closed attempts stopped before the exclusive output open and left
the create-once path absent. They exposed construction-integrity defects, not
adaptive results:

1. the initial metadata rule incorrectly rejected the workstation's preserved
   mode-`0664` repository files;
2. the public-task binding reconstructed evaluator replicate labels as
   `00/01` instead of the frozen producer's `01/02`; and
3. the removal rejoin preferred a decision-evidence reference where the
   frozen producer records the reservation reference.

Each correction was restricted to exact producer/input identity semantics,
received a direct positive and tamper-negative test, and passed a fresh
read-only audit. No task, model output, score, threshold, arm, seed, learned
state, or scientific classification was changed. The fourth attempt created
the sole result. The identity is now permanently consumed and must not be
rerun.

## Reconstructed population and provenance

The diagnostic rejoined:

- 468 unique finalized attempt receipts and 468 managed-runtime dispositions;
- 433 admitted and 35 malformed attempts;
- all 60 frozen removal-fairness rows;
- four persistent cognitive lineages and 96 retained cognitive records;
- exactly three successful final `QWEN_ONLY` task IDs and their seven-arm
  same-task peers, producing the unique 21-attempt matrix; and
- 36 target recall occurrences, 24 unique scope-local retained records, two
  target `FULL` lineages, and one raw record-reference collision across
  scopes.

All 36 recall occurrences rejoined exact retained record bytes, content,
acquisition reference, source reference, lineage scope, and adaptation
ancestry. The cross-scope collision confirms that provenance identity must
remain `(lineage_scope_ref, record_ref)`, not a global raw `record_ref` key.

The diagnostic did not recompute the complete `FrozenRecallBatch` preimage or
the full prospective contract. Those claims remain explicitly false, with
prospective recomputation `INDETERMINATE`. It did establish persisted
reference/hash/content joins and the legacy reservation-to-observed-episode
adaptation ancestry required by this leaf.

## Same-task trace matrix

All 21 target attempts were admitted by the proposal parser. The three
`QWEN_ONLY` attempts each selected a declared-grammar-conformant procedure,
executed exactly that selected text, produced a conformant response, and
received objective success.

Every one of the 18 history-bearing peer attempts received objective failure.
For each history arm across the three tasks:

- recall count was exactly 12;
- the first recorded divergence from its `QWEN_ONLY` peer was the history
  preimage, not proposal admission;
- none executed text byte-identical to its selected procedure;
- selected procedure classes were one declared malformed shape and two values
  outside the declared grammar; and
- all three executed responses were malformed under the already-public glyph
  response grammar.

For every target task, `FULL`, `RETRIEVAL_ONLY`, `FROZEN_ORIGIN`,
`PROSPECTIVE_REMOVAL`, and `BACKEND_REMOVAL` had exact-equal recall
preimages, proposal requests, and proposal sets. `RANDOM_FEEDBACK` had a
different retained lineage/preimage and proposal set, but also failed.
`QWEN_ONLY` alone had no persistent history and succeeded. The six
history-bearing arms shared the same zero-success outcome for this cohort.

This establishes an upstream observational boundary: the integrated failures
were already separated from the successful controls at history conditioning,
and their selected procedure material was not grammar-conformant. It does not
establish that Cognee, Moving Origin, prospective dynamics, or learned state
is generally harmful. The R2 removals deliberately preserve frozen recall,
and therefore cannot isolate prompt occupancy, recall formatting, record
relevance, cross-family contamination, or useful versus harmful memory
content.

## Diagnostic question dispositions

| Question | Disposition | Bounded interpretation |
|---|---|---|
| Q1 | `ESTABLISHED` | Exactly three final `QWEN_ONLY` successes and one unique 21-attempt seven-arm matrix rejoined. |
| Q2 | `ESTABLISHED` | Every target trace and its first exact divergence was reconstructed. |
| Q3 | `RULED_OUT` | Proposal-parser rejection is not the proximate explanation for these 21 admitted attempts. |
| Q4 | `CONSISTENT_WITH_EVIDENCE` | The frozen removals and equality groups support, but do not causally prove, a history-conditioning bottleneck. |
| Q5 | `RULED_OUT` | Target provenance rejoined, and none of the three objective successes had a selected-versus-executed grammar-class mismatch. |
| Q6 | `INDETERMINATE` | R2 lacks the isolating intervention needed to distinguish history length/format from relevance/content and learned-lineage effects. |

The result therefore rules out the earlier success-congruence mismatch
hypothesis for the three `QWEN_ONLY` wins. A generic selected-to-executed
congruence witness remains useful as a shadow measurement because 12 of the
18 history-arm peers differed in grammar class and all 18 differed in exact
text, but this evidence does not justify using that witness as a corrective
filter or positive-credit gate.

## Resources and privacy

The one-process diagnostic reported 641 milliseconds, 920,752,128 bytes peak
RSS, 34,947,196 input bytes, and a 100,017-byte output. These values remained
below the frozen 120-second, 1-GiB-RSS, and 1-MiB-output ceilings. The durable
document contains only closed schemas, references, enums, booleans, counts,
indices, lengths, equality relations, and dispositions. Independent
reconstruction found no raw task, prompt, proposal, selected trace, response,
recalled text, seed, answer, solution, mechanism, route, token IDs, database
row, or reversible encoding of the 1,280 captured raw values.

## Gate and next exact action

`ANG-GATE-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-R2-EVIDENCE-TRACE-DIAGNOSIS-001`
passes. The result is a bounded explanation of the consumed R2 evidence; it
does not alter `NOT_SUPPORTED` and does not establish improved reasoning,
general incapability, selfhood, autonomy, readiness, or AGI.

The smallest scientifically useful successor is a fresh preregistered
history-conditioning isolation experiment with new seeds and identity. It
should retain identical frozen-model and objective-evaluator boundaries while
separating at least:

- no history;
- the ordinary frozen 12-record history;
- an equal-slot/equal-budget neutral history-format control;
- a precommitted shuffled or cross-family history control; and
- a same-family/relevance-stratified history intervention used only as an
  experimental control, not a deterministic task router.

`FULL`, Qwen-only, retrieval/runtime removals, and fair resource accounting
must remain available where needed to preserve the integrated claim. A
tri-state procedure/outcome congruence witness may run in shadow mode only.
The fresh protocol must preregister prompt/token equality, record-selection
rules, thresholds, controls, and stop conditions before any new seed or model
result is observed. It may not reuse R2 identities, tune against these three
tasks, encode a task solution, or present an observational association as a
causal effect.
