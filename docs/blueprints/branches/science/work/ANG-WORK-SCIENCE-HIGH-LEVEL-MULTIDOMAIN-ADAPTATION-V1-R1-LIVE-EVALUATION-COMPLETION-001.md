# ANG-WORK-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-R1-LIVE-EVALUATION-COMPLETION-001

Status: ready for pre-seed construction; no R1 seed, manifest, release, result,
state, scratch, evaluation admission, model process, or Cognee scope exists

Tier: 4 experimental construction-completion work leaf

Active node: `ANG-BP-SCIENCE`

Parent: `ANG-BP-SCIENCE`

Predecessor:
`ANG-WORK-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-R1-001` at
SHA-256 `d6f1abb75d356d5f11896e873ce7caba0db9cc253304cd08cbe42ee324199900`.
Its exact malformed-random-feedback recovery construction passed focused and
independent audit, but its copied runner deliberately retained the V1
qualification-only public CLI and therefore cannot complete its declared
contingent evaluation. No live R1 identity has been consumed.

Gate:
`ANG-GATE-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-R1-LIVE-EVALUATION-COMPLETION-001`
— NOT RUN

## Accountable outcome

Complete and independently audit the missing zero-injection production owner
and CLI for the already-declared R1 one-use evaluation before any R1 seed is
sealed. The resulting source-complete runner must remain fail-closed until an
exact R1 `QUALIFICATION_PASS`, then durably admit and execute the existing
frozen evaluation path without an operator gap.

This leaf completes an omitted production boundary. It does not create a new
experiment, alter the sole malformed-random-feedback correction, change a
task/model/learner/evaluator/arm/seed-selection/threshold/budget semantic, add
a retry or repair, encode a task solution, or authorize a live run by itself.

## Inherited invariants and assurance

All requirements, identities, immutable V1 witnesses, paths, resource ceilings,
stop conditions, scientific claim limits, deterministic-code restrictions,
Human-Flourishing constraints, and rollback behavior in the predecessor are
inherited without weakening. In particular:

- deterministic code remains limited to orchestration, evidence integrity,
  objective judging, resource measurement, and safety boundaries;
- final evaluation seeds and payloads remain hidden inside the evaluator
  worker and are never available to the model-facing runtime;
- evaluation admission remains impossible unless the durable R1 qualification
  result is exactly `QUALIFICATION_PASS` and every manifest/source/seed/
  evaluator commitment revalidates;
- the admitted evaluation runs adaptation, development, and final
  automatically with the existing frozen schedule and no operator gap;
- one assigned RTX 5080, offline same-host workers, synthetic data, no users,
  no network/provider/telemetry, no external action, and no promoted-state
  mutation remain exact; and
- neither qualification nor evaluation is an approved Action,
  `EvaluationReceipt`, `PromotionDecision`, deployment authorization, or claim
  of general intelligence.

The inherited-plus-completion impact is LOW under
`ANG-CTR-HUMAN-IMPACT-ASSESSMENT-001` and remains mapped to
`ANG-GATE-HUMAN-FLOURISHING-001`. The new code only closes a local execution,
ownership, integrity, and cleanup boundary already required by the experiment.

## Exact pre-seed defect

The audited R1 runner at SHA-256
`dab22dfbd0addc9d2c33c7108b423383f34d7401929a5a12a1ce0aa0b041b23c`
contains the generic admission coordinator and complete injected evaluation
engine, but:

1. `run_and_publish_evaluation_once` requires caller-supplied dependency and
   evaluator-handshake callbacks;
2. no exact live evaluation dependency owner or public zero-injection wrapper
   exists; and
3. `main(("--evaluation",))` deliberately raises instead of entering the
   one-use path, with a unit test locking that closure.

The qualification path creates a manifest that hashes the R1 runner and both
R1 test sources. Adding the missing owner after qualification would therefore
invalidate the frozen manifest. Supplying callbacks externally would consume a
one-use identity through code not bound by that manifest. Construction must be
completed and audited now, while every R1 live path is absent.

## Authorized construction delta and literal scope

Repository writes are limited to this leaf and the three already-declared R1
construction sources:

- `experiments/runners/high_level_multidomain_v1_r1.py`;
- `tests/unit/experiments/test_high_level_multidomain_runner_v1_r1.py`; and
- `tests/integration/runtime/test_high_level_multidomain_qwen_v1_r1.py`.

The active-leaf path/hash binding may move from the predecessor leaf to this
leaf. The exact 29 frozen predecessor sources plus three dynamic R1
construction sources remain the complete manifest inventory.

Within those files, only the following additional behavior is authorized:

1. declare R1-disjoint evaluation runtime, genesis, evidence-ledger,
   arm-factory, and Cognee-scope paths beneath the existing R1 state root;
2. add one exact production evaluation dependency owner, constructed only
   after the durable evaluation-admission permit is consumed, that revalidates
   the repository, manifest, seed commitments, evaluator commitments, parent
   boundary, frozen Qwen files/tensors, learner genesis, resource ceilings,
   owned paths, and cleanup ownership;
3. add one bounded commitment-only evaluator handshake that closes and reaps
   before the admission claim is created, followed by a fresh full evaluator
   bound to the admitted final digest;
4. add a zero-argument public evaluation entrypoint and wire only
   `--evaluation` to it; retain injected seams solely as private CPU/fake test
   boundaries; and
5. add focused tests/comments proving admission-before-construction, exact
   path and payload bindings, two-replicate genesis/lineage construction,
   evaluator/Qwen/arm/repository cleanup on success and failure, create-once
   publication, CLI non-injectability, and unchanged scientific semantics.

The production owner should reuse the already-tested evaluator, Qwen,
`FreshGenesisBundle`, `HighLevelArmFactory`, `EvidenceLedger`, resource sampler,
and owner-transfer mechanisms. Copy-pasted evaluator logic, a second scoring
path, new task parsing, a task/world solver, an external launcher carrying
callbacks, or a post-qualification source edit is forbidden.

No R1 live path may be created and no seed, manifest, release, admission,
result, GPU/model process, Cognee worker, or network operation may occur during
construction or audit.

## Acceptance tests and evidence

Before seed sealing:

1. the predecessor normalized R1-to-V1 delta gate still proves the sole
   scientific-semantic correction is the malformed-random-feedback condition;
   the new allowed delta is classified only as live evaluation orchestration,
   integrity, resource, ownership, cleanup, and tests;
2. exact static checks prove all evaluation paths are absolute, under the R1
   write scope, pairwise disjoint from qualification-owned mutable roots, and
   absent before live execution;
3. a fake-model/fake-Cognee live-owner test constructs both evaluation
   replicate lineages only after a valid consumed permit payload, binds the
   exact evaluator commitments and final-admission digest, finalizes evidence,
   and audits every owner;
4. pre-admission failure, non-PASS qualification, source/manifest/seed/
   handshake drift, malformed owner payload, partial construction failure,
   model-load failure, run failure, cleanup failure, and result-publication
   failure remain fail-closed with no unauthorized continuation;
5. the public evaluation function has no callback/path parameters and
   `main(("--evaluation",))` calls only that zero-argument function;
6. the full copied unit and exact-Qwen integration suites pass with CUDA hidden
   and warnings fatal, together with unchanged focused evaluator/runtime/store/
   Cognee/prospective suites, compile, import, no-solver/no-answer, retained V1
   witness, source inventory, and no-index whitespace checks; and
7. an independent read-only audit accepts the exact active-leaf, runner, unit,
   and integration hashes and confirms every R1 live path remains absent.

Only after all seven conditions pass may the predecessor's exact live sequence
resume: prepare fresh `0700` roots, seal fresh R1 evaluation seeds once,
create/rejoin the source-complete manifest, execute one qualification, preserve
and independently audit its terminal result, and execute evaluation only on an
exact audited `QUALIFICATION_PASS`.

## Gate, rollback, and next exact action

This construction-completion gate passes only on the declared tests and
independent audit. It does not pass the scientific experiment gate and cannot
admit evaluation without the separate durable qualification result.

Before seed sealing, rollback is limited to removing this unconsumed leaf and
restoring only the three R1 construction files to their predecessor-audited
hashes. After any R1 seed or live artifact exists, preserve all bytes and use a
new explicitly versioned recovery identity for any further correction. V1 is
never a rollback target.

Next exact action: implement the zero-injection live evaluation owner and CLI
only in the declared R1 runner/tests, run the focused fake/integrity suites,
then obtain an independent exact-hash audit. Do not prepare live roots or seal
seeds yet.
