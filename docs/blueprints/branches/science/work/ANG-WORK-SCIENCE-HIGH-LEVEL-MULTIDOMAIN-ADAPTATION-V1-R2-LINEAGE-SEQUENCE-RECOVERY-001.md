# ANG-WORK-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-R2-LINEAGE-SEQUENCE-RECOVERY-001

Status: ready for bounded pre-seed construction; every R2 code, test, manifest,
report, state, scratch, seed, release, admission, result, model-process, and
Cognee-scope path is absent

Tier: 4 experimental recovery work leaf

Active node: `ANG-BP-SCIENCE`

Parent: `ANG-BP-SCIENCE`

Consumed predecessor:
`ANG-WORK-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-R1-LIVE-EVALUATION-COMPLETION-001`
at SHA-256
`a54aa78d48fe5435b035e3c057470c915b87c8f4a4de9d001997a1dcd57d39de`.
Its one-use qualification terminated as a valid technical
`QUALIFICATION_FAILURE`; R1 evaluation was not admitted and R1 must never be
edited, resumed, or rerun.

Gate:
`ANG-GATE-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-R2-LINEAGE-SEQUENCE-RECOVERY-001`
— NOT RUN

## Accountable outcome

Correct exactly one aggregate lineage-continuity baseline contradiction,
prove that the correction binds rather than weakens the qualified transaction
history, and execute one fresh versioned R2 qualification/evaluation identity
while preserving every V1 and R1 byte, threshold, and failure record.

This is recovery of the already-frozen integrated experiment. It is not a new
scientific design, task or prompt change, learner change, threshold revision,
solver addition, retry, interpretation of partial R1 behavior, or permission
to repeat a consumed identity.

## Inherited invariants and assurance

All requirements, evaluator logical identities, task families, partitions,
arms, schedules, budgets, thresholds, model and learner identities, parser and
feedback rules, removal controls, resource ceilings, stop conditions,
deterministic-code restrictions, nonauthorization meanings, and scientific
claim limits from the V1, R1, and R1 construction-completion leaves are
inherited without weakening. In particular:

- frozen Qwen remains the sole language/knowledge substrate and its tensors
  remain immutable;
- Cognee supplies scoped acquisition and recall, Moving Origin supplies
  situated temporal state, and Angler supplies procedural adaptation and
  outcome credit; none is replaced by deterministic task logic;
- deterministic code remains limited to generic orchestration, integrity,
  replay, objective judging, resource measurement, and safety boundaries;
- a malformed proposal consumes its attempt and produces no execution or
  learner transition;
- final payloads remain evaluator-only until construction-bound admission;
- no threshold, task, arm, ordering, count, seed-selection rule, budget,
  parser, feedback scalar, scoring rule, prompt, or acceptance meaning may
  change after viewing R1 evidence; and
- neither this recovery nor a successful bounded result is an approved
  Action, `EvaluationReceipt`, `PromotionDecision`, deployment authorization,
  or claim of AGI, self-awareness, feelings, autonomy, readiness, or general
  reasoning improvement.

Assurance remains LOW under `ANG-CTR-HUMAN-IMPACT-ASSESSMENT-001`: local
synthetic data, one assigned RTX 5080, offline same-host workers, no users or
subjects, no network/provider/telemetry, no external action, and no
promoted-state mutation. This inherited-plus-delta assessment remains mapped
to `ANG-GATE-HUMAN-FLOURISHING-001` because it preserves truth in claims,
meaningful human control, exact stop boundaries, and immutable failure
evidence. No ADR is required because the correction is local to an
experimental evidence validator and changes no shared interface or safety
boundary.

## Immutable R1 failure evidence

The R1 result report is
`docs/reports/HIGH_LEVEL_MULTIDOMAIN_ADAPTATION_V1_R1_RESULT.md` at SHA-256
`114fd31e35e4bb0778eaeb7b25421c41b297b1b443a3dc95d5b5d6755954be7f`.
The following predecessor bytes and tree identities are permanent R2 inputs:

| Artifact | SHA-256 |
|---|---|
| R1 active leaf | `a54aa78d48fe5435b035e3c057470c915b87c8f4a4de9d001997a1dcd57d39de` |
| R1 runner | `78152a1502ca8317c16c9aa77f913d46ec031229fa51e8aaf2d0de25f2c2398a` |
| R1 unit suite | `9a929950b7f64b0f8220716f39187040ab52f7e3198dd92a39794c72b624e372` |
| R1 exact-Qwen integration | `bb8aa7e931b6ab197741a56d7a54de68f975fcbb2cc1c9c4840424ff11b9cc43` |
| R1 seed seal | `4f7360f91a82c57a0212bb9fa5e95acd743d84b63a8d08bb32c7d724451f0d4f` |
| R1 release claim | `e5c4483e7162244bb5c2101961e253a94c8f628c44599cad4807ed2959683c68` |
| R1 source manifest | `2b8adadf402d52f1c0cd7a4523c855a0951fc04333fe43350009b1a8bd72c36d` |
| R1 terminal result | `547dbce00c9175fb97d4abcdf61cc8fb8ad0c2dd91f293c350a7c4700814c16c` |
| R1 attempt ledger | `f167a08029baa46eeff7c028e73c3e16d4f5973c5bbd5234fe6a4da61c0190f5` |
| R1 arm-factory audit | `01cd9c91e2407584e4b5a9db5dc9f818029f7c9341cac176121507cabd460183` |
| R1 result report | `114fd31e35e4bb0778eaeb7b25421c41b297b1b443a3dc95d5b5d6755954be7f` |
| R1 retained state tree | `136f04653ed9dc32b1d8eff6c09921b12ab7ca6b258a33fde9f58e714a46a30c` |
| R1 retained scratch tree | `a9191c6653c9b5417c80e1779e821e318768a3dba8a732313d821bbbf979d43f` |

The state-tree inventory is 53 files, 36 directories, 11,487,875 logical
bytes, and 89 records. The scratch-tree inventory is 65 files, 14 directories,
812,194 logical bytes, and 79 records. Both use the inherited
`angler.local-tree-digest.v1` algorithm. R1 evaluation runtime, evaluation
Cognee scopes, admission claim, and result remain absent. All V1 immutable
witnesses remain inherited and must also rejoin.

The R1 public seed commitments are
`sha256:869675ad3b43bdd300f6ed58b8bf0af2b87520318b42e36d721565a7b7e9d9f5`
and
`sha256:44ea52eb9a4085e0e803a965b53ad0fa6ac58cd642fc82ae130a583371b9316f`.
Their raw seeds remain sealed, may not be inspected or reused, and do not
authorize R1 evaluation.

## Exact defect and sole semantic correction

Each qualified persistent lineage starts with transaction-store genesis at
sequence 0, then commits exactly one neutral unrelated bootstrap episode at
sequence 1 without changing the step-0 learner genesis digest. Every first
admitted scheduled learner transition must therefore be transaction sequence
2. The consumed R1 ledger shows continuous admitted sequences 2 through 11 in
both `FULL` and `RANDOM_FEEDBACK`, with malformed attempts correctly carrying
no transition.

The live and serialized aggregate lineage validators both incorrectly set
`previous_sequence = 0`. The only permitted scientific-semantic correction is
to introduce one exact qualified baseline constant equal to 1 and bind all
three sides of the invariant:

1. the neutral bootstrap commit must return transaction sequence 1 and a head
   at sequence 1 with the unchanged qualified genesis state digest;
2. live adaptation-lineage validation starts from that exact bound sequence;
3. serialized adaptation-lineage validation starts from the same exact bound
   sequence.

Thus the first admitted transition must be sequence 2. The validator must
reject starts at 1 or 3, arbitrary offsets, gaps, duplicates, resets, forks,
unchanged child digests, malformed attempts with transitions, and admitted
attempts without transitions. No schema field, durable store behavior,
bootstrap content, task/model/learner behavior, or scientific metric changes.

## Fresh R2 identities, qualification seed, and literal paths

The evaluator suite schema and its internal logical qualification/evaluation
identities remain frozen inputs. Fresh run identities are:

- qualification run:
  `angler.high-level-multidomain.v1-r2-harness-qualification`;
- evaluation run: `angler.high-level-multidomain.v1-r2-evaluation`.

The public qualification seed remains exactly `2026083190` for this one R2
technical recovery identity. This scoped reuse re-exercises the same fixed
qualification schedule after the sole aggregate-validator correction; it does
not reopen V1 or R1, permit threshold tuning, expose final payloads, or
authorize later reuse. R2 evaluation uses two new 256-bit operating-system
entropy seeds whose commitments must be distinct from every V1 and R1
commitment. No prior raw seed may be read, copied, or transformed.

Repository outputs are limited to:

- this leaf;
- `experiments/runners/high_level_multidomain_v1_r2.py`;
- `tests/unit/experiments/test_high_level_multidomain_runner_v1_r2.py`;
- `tests/integration/runtime/test_high_level_multidomain_qwen_v1_r2.py`;
- create-once `experiments/manifests/high-level-multidomain-v1-r2.json` only
  after seed sealing;
- `docs/reports/HIGH_LEVEL_MULTIDOMAIN_ADAPTATION_V1_R2_RESULT.md` only after a
  terminal R2 result; and
- one append-only R2 gate disposition in `AGENTS_SYNC.md` after independent
  result audit.

Live outputs are limited to:

- `/opt/angler/state/project-angler/high-level-multidomain-v1-r2/**`;
- `/opt/angler/scratch/high-level-multidomain-v1-r2/**`;
- `/opt/angler/results/high-level-multidomain-v1-r2-qualification.json`; and
- `/opt/angler/results/high-level-multidomain-v1-r2.json`.

All R2 state, scratch, seed, manifest, release, admission, runtime, Cognee
scope, temporary, and result paths must be literally disjoint from V1 and R1.
No cleanup, overwrite, alias, symlink, hardlink, or copied live state may be
used to manufacture freshness.

## Construction scope and source binding

Mechanically copy the exact frozen R1 runner, full unit suite, and exact-Qwen
integration suite into the three R2 construction paths. Permitted differences
are bounded to:

1. the exact bootstrap-sequence binding and two validator initializations;
2. R2 repository/live paths, run/result/release identities, source inventory,
   and fresh-seed non-overlap checks;
3. immutable R1 artifact and retained-tree witnesses; and
4. tests/comments proving those exact differences.

The R2 source manifest must retain all 32 source hashes from the consumed R1
manifest, freeze the R1 completion leaf, R1 source manifest, and R1 result
report as additional predecessor sources, and add exactly the three dynamic R2
construction sources. The R2 active leaf remains separately bound by its exact
hash. A normalized R2-to-R1 delta gate must remove only declared identity/path/
source substitutions, immutable-witness additions, baseline-binding code, and
their tests/comments before requiring the complete R1 source to rejoin.

Non-goals include editing R1, changing runtime/store/Cognee/learner/bootstrap
behavior, changing the evaluator, tuning Qwen prompts, altering parser
strictness, adding retries or repairs, changing deterministic task logic,
interpreting partial R1 scores, or advancing evaluation without exact R2
qualification PASS.

## Acceptance tests and evidence

Before any R2 live path or seed exists:

1. exact validator tests prove bootstrap sequence 1 followed by admitted
   sequence 2 passes in both live and serialized paths, while starts at 1 or 3,
   gaps, duplicates, resets/forks, unchanged children, malformed-with-
   transition, and admitted-without-transition fail;
2. a real-baseline fake-model/fake-Cognee integration proves the neutral
   bootstrap owns head sequence 1 and the first learned transition is sequence
   2 without changing task, model, learner, or store behavior;
3. all immutable R1 artifact hashes, R1 state/scratch tree digests, R1
   evaluation-path absence, and inherited V1 witnesses rejoin;
4. the normalized mechanical-delta gate proves no source change outside the
   declared substitutions, sequence binding, and tests/comments;
5. the full copied R2 unit and exact-Qwen integration suites pass with CUDA
   hidden and warnings fatal;
6. unchanged evaluator, runtime/store, Cognee, prospective, compile, import,
   no-solver/no-answer, hidden-material, resource, cleanup, repository/source
   mutation, and no-index whitespace checks pass; and
7. an independent read-only audit accepts the exact active-leaf, runner, unit,
   and integration hashes and confirms every R2 live path remains absent.

Only after all seven conditions pass may the live sequence begin: prepare
fresh empty owned mode-`0700` R2 roots and declared scratch directories; seal
fresh R2 evaluation seeds once; create/rejoin the source-complete manifest;
claim and execute the one-use 31-arm R2 qualification; preserve and
independently audit its terminal result; and execute evaluation only if that
result is exactly `QUALIFICATION_PASS` and every source, foundation, resource,
cleanup, seed, evaluator, repository, and admission guard revalidates.

## Gate, rollback, and next exact action

The R2 gate passes only when the declared construction evidence, one-use R2
qualification, contingent one-use R2 evaluation, independent result audit, and
inherited Human-Flourishing boundary all pass. A valid `NOT_SUPPORTED`
evaluation completes the experiment without supporting the hypothesis. Any
technical failure is preserved and cannot authorize a rerun or evaluation.

Before seed sealing, rollback is limited to removing only the unconsumed R2
leaf and three R2 construction copies. After any R2 seed or live artifact
exists, preserve every byte and use another explicitly versioned identity if
justified. V1 and R1 are never rollback targets.

Next exact action: mechanically create only the three R2 code/test copies,
apply the literal R2 identity/path/source substitutions and exact bound
bootstrap-sequence correction, then run the focused sequence and normalized-
delta tests. Do not prepare R2 live roots, seal seeds, create a manifest, or
start model/GPU/Cognee processes yet.
