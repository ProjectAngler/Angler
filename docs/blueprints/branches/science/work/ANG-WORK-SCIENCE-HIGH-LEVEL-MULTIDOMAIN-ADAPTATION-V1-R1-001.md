# ANG-WORK-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-R1-001

Status: ready for bounded recovery construction; no R1 seed, manifest, release,
result, state, scratch, or evaluation admission exists

Tier: 4 experimental recovery work leaf

Active node: `ANG-BP-SCIENCE`

Parent: `ANG-BP-SCIENCE`

Predecessor: `ANG-WORK-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-001`.
Its qualification identity is consumed and failed for a deterministic evidence
validator contradiction before development. Its gate remains not passed.

Gate: `ANG-GATE-SCIENCE-HIGH-LEVEL-MULTIDOMAIN-ADAPTATION-V1-R1-001` — NOT RUN

## Accountable outcome

Correct exactly one malformed-random-feedback evidence rule, prove the
correction without changing task/model/learner semantics, and execute one fresh
versioned qualification/evaluation identity while preserving every V1 byte and
threshold.

This is recovery of the already-designed integrated experiment, not a fresh
scientific design, a solver change, a threshold revision, or permission to
repeat the consumed V1 identity.

## Inherited invariants and assurance

All invariants, non-equivalence rules, deterministic/evaluator boundaries,
task families, arms, fairness controls, model and learner identities, resource
ceilings, stop conditions, and scientific claim limits from the predecessor
leaf are inherited without weakening. In particular:

- the foundation model remains frozen;
- deterministic code remains limited to orchestration, evidence integrity,
  objective judging, resource measurement, and safety boundaries;
- no parser may repair, retry, infer, add, or reorder a model proposal;
- a malformed proposal consumes its attempt, scores zero where judged, and
  performs no execution or learner update;
- final payloads remain evaluator-only until construction-bound admission;
- no threshold, arm, order, count, budget, seed-selection rule, or acceptance
  meaning may change after the V1 qualification evidence was viewed; and
- neither this recovery nor a successful result is an approved
  `EvaluationReceipt`, `PromotionDecision`, normal SCIENCE gate pass,
  deployment authorization, or claim of general intelligence.

Assurance remains LOW: local synthetic data, offline same-host processes, one
assigned GPU, no users or subjects, no network/provider/telemetry, no external
action, and no promoted-state mutation. This proportional recovery remains
mapped to `ANG-GATE-HUMAN-FLOURISHING-001` because it preserves meaningful
human control, exact stop boundaries, truth in claims, and immutable failure
evidence without introducing human data or external effects.

This inherited-plus-delta LOW assessment is the R1 gate's explicit
`ANG-CTR-HUMAN-IMPACT-ASSESSMENT-001` input; satisfying it and
`ANG-GATE-HUMAN-FLOURISHING-001` is a prerequisite to the R1 experimental
gate and cannot be waived by the runner or learner.

## Immutable V1 predecessor evidence

The following bytes are rollback and non-mutation witnesses:

| Artifact | SHA-256 |
|---|---|
| V1 leaf | `b8678803ac2ea63f44d9f1e386dffefb907be619d208c18aba71de31e39e629a` |
| V1 runner | `4c9dc1551d5902f4132ac8d7262a025c17d72299de79d872f0b300d0299356d7` |
| V1 runner tests | `5a225fdd57b8690cf33b63eed5d0d8cf25d575ee46d929a7e9e67f7329d9ae92` |
| V1 exact-Qwen integration | `05836ee77994f8905a8a2523135872a81b8b67bcdae77e102e730ac9ef55025d` |
| V1 source manifest | `91542cc7557495c7793f82247772e71b0f00f6995e3b4b3fbf2185ee38b470f8` |
| V1 seed seal | `74e035d78a27c7ddcafe8702b9cb1e35e5b9c5e5816e22f4d65d5b1d46d6d5bc` |
| V1 release claim | `9002e17cc58de95796cc2dd1812509a68e3a9c81e1dcc9c0b4a2def54c6eaa61` |
| V1 terminal result | `feae381e64781d458e2cce5661e63393d165680964a2de24739cc9e24a4d34e1` |
| V1 result report | `e532ecaba5c8efa2d479531c9a9686dbe80ad7589f3ee4058f0be18cfe38ad97` |
| V1 retained state tree | `f23bda8dfb077450aaed47bc2ebb920d5594fef310f169ff0f76967f943a6cbb` |
| V1 retained scratch tree | `362c077ca959b55a44fd322df25ab468a79914013119d9ecb8a1107171a04062` |

The V1 report is
`docs/reports/HIGH_LEVEL_MULTIDOMAIN_ADAPTATION_V1_RESULT.md`. The V1 result is
a valid canonical `QUALIFICATION_FAILURE` with `scientific_claim=false`. It
contains 12 staged and 11 finalized adaptation attempts; no development,
final, evaluation admission, or scientific metric was reached. V1 artifacts
must never be reset, cleaned, resumed, copied into R1 state, rewritten, or
reinterpreted as a pass.

The two retained-tree values use `angler.local-tree-digest.v1`. Enumerate the
root itself as relative path `.` followed by every descendant in ascending
POSIX relative-path order. Reject symlinks, special files, and regular files
whose link count is not one. Encode each record as canonical compact ASCII JSON
plus LF:

```text
[relative_path, "directory"|"file", mode_integer, uid, gid,
 logical_size, file_sha256_or_null]
```

Directory logical size is zero and its digest is null; regular-file logical
size is `st_size` and its digest is lowercase SHA-256 of its bytes. SHA-256 of
the concatenated record bytes is the tree digest. The frozen state inventory is
28 files, 24 directories, 6,384,237 logical bytes, and 52 records. The frozen
scratch inventory is 57 files, 13 directories, 728,211 logical bytes, and 70
records.

## Exact defect and sole semantic correction

The twelfth V1 stage was a malformed `RANDOM_FEEDBACK` diagnostic. The evidence
validator correctly required a malformed attempt to have neither feedback nor
a learner transition, then incorrectly required every random-feedback
diagnostic to have both. The only permitted semantic code change is:

```python
if random_diagnostic and not malformed and (not feedback or not transition):
    raise ValueError("random-feedback diagnostic lacks its learner transition")
```

Thus an admitted random-feedback attempt still requires its exact feedback and
transition, while a malformed random-feedback attempt still forbids both. No
task, model, selection, execution, feedback, update, parser, retry, scoring, or
threshold behavior changes.

## Fresh R1 identities and literal paths

The evaluator suite schema and its internal logical commitment identities
remain unchanged because the evaluator is a frozen input. The one-use run
identities are separately versioned:

- qualification run:
  `angler.high-level-multidomain.v1-r1-harness-qualification`;
- evaluation run: `angler.high-level-multidomain.v1-r1-evaluation`.

The R1 runner must keep the frozen evaluator identities distinct from these
run/result/release identities and validate both explicitly. R1 uses two fresh
256-bit operating-system-entropy evaluation seeds. It must never copy or reuse
the V1 raw seeds or commitments. The public qualification seed remains exactly
`2026083190`, so task/schedule construction and qualification coverage are
unchanged.

Scoped recovery supersession: the predecessor's statement that public
qualification seed `2026083190` belongs only to the consumed V1 qualification
is superseded solely for this one R1 technical recovery identity. Reusing the
public seed deliberately re-exercises the exact malformed-output boundary that
the harness mishandled; it does not reopen or resume V1, admit evaluation, or
authorize any later reuse. All final-evaluation seed material remains fresh and
unseen.

Repository outputs:

- `experiments/runners/high_level_multidomain_v1_r1.py`;
- `tests/unit/experiments/test_high_level_multidomain_runner_v1_r1.py`;
- `tests/integration/runtime/test_high_level_multidomain_qwen_v1_r1.py`;
- create-once `experiments/manifests/high-level-multidomain-v1-r1.json` after
  the R1 seeds are sealed;
- `docs/reports/HIGH_LEVEL_MULTIDOMAIN_ADAPTATION_V1_R1_RESULT.md` only after a
  terminal R1 result; and
- one append-only R1 gate disposition in `AGENTS_SYNC.md` after independent
  result audit.

Live outputs are limited to:

- `/opt/angler/state/project-angler/high-level-multidomain-v1-r1/**`;
- `/opt/angler/scratch/high-level-multidomain-v1-r1/**`;
- `/opt/angler/results/high-level-multidomain-v1-r1-qualification.json`; and
- `/opt/angler/results/high-level-multidomain-v1-r1.json`.

Every R1 state, scratch, seed, release, admission, runtime, Cognee scope,
manifest, temporary, and result path must be literally disjoint from every V1
path. No auto-cleanup or overwrite may be used to manufacture freshness.

## Construction scope and non-goals

Mechanically copy the exact V1 runner, full runner unit suite, and exact-Qwen
integration suite into the three R1 paths. Permitted differences are bounded
to:

1. the one validator condition above;
2. R1 repository/live paths and construction-source inventory;
3. explicit separation of frozen evaluator identities from fresh R1
   run/result/release identities; and
4. tests and comments that prove those exact differences.

Add the consumed V1 leaf, runner, unit test, integration test, and source
manifest hashes as frozen R1 predecessor inputs. The R1 construction-source map
contains exactly the R1 runner, R1 unit test, and R1 integration test in place
of their V1 construction counterparts. All other frozen source hashes remain
unchanged.

Non-goals include fixing the generic qualification-result loader, changing a
cleanup classifier, modifying V1, changing the evaluator, changing Cognee or
runtime behavior, tuning Qwen prompts, altering parser strictness, adding a
retry, changing deterministic task logic, interpreting partial V1 scores, or
advancing evaluation without R1 qualification PASS.

## Acceptance tests and evidence

Before any R1 seed is created:

1. a direct fresh temporary-ledger test proves a malformed random-feedback
   diagnostic with no feedback/transition finalizes and audits, an admitted
   diagnostic without either still fails, and malformed evidence cannot
   launder either;
2. an exact fake-model/fake-Cognee managed attempt proves one malformed
   proposal causes one generation, zero execution/update, one finalized row,
   complete disposition/lineage cleanup, and a passing factory audit;
3. the copied V1 runner and exact-Qwen suites pass against the R1 module;
4. a normalized mechanical-delta gate proves no runner change outside the
   declared path/identity/source substitutions, one validator condition, and
   their tests/comments;
5. exact static assertions prove all R1 paths are pairwise correct and disjoint
   from V1, all immutable V1 hashes and retained tree digests rejoin, and the
   frozen thresholds/schedule/arms/budgets/model/evaluator inputs are unchanged;
6. unchanged evaluator, runtime/store, Cognee, prospective, compile, import,
   no-solver/no-answer, hidden-material, resource, cleanup, source/repository
   mutation, and no-index whitespace checks pass; and
7. an independent read-only audit accepts the exact R1 hashes and normalized
   delta before seed sealing or live resource use.

The live sequence is then exactly: prepare fresh empty owned mode-`0700` R1
roots and declared scratch directories; seal the fresh R1 seed artifact once;
create/rejoin the commitment-only R1 manifest once; claim the R1 qualification
once; execute the unchanged 31-arm qualification; preserve and independently
audit its terminal result. Evaluation admission remains fail-closed unless the
R1 qualification result is exactly `QUALIFICATION_PASS` and all source,
foundation, resource, cleanup, and repository guards rejoin.

## Gate, rollback, and next exact action

The R1 gate passes only when the declared construction/static/focused evidence,
one-use R1 qualification, contingent one-use R1 evaluation, independent result
audit, and inherited Human-Flourishing boundary all pass. A valid
`NOT_SUPPORTED` evaluation completes the experiment without supporting the
hypothesis. A technical failure is preserved and cannot authorize a rerun or
evaluation.

Before R1 seed sealing, rollback removes only the three unconsumed R1
construction copies and this leaf. After seed sealing, manifest creation,
release, or a result, preserve every R1 artifact and use another explicitly
versioned recovery identity if justified. V1 is never a rollback target.

Next exact action: mechanically create only the three R1 code/test copies,
apply the literal identity/path/source substitutions and sole validator fix,
then run the focused normalized-delta and malformed-random-feedback tests. Do
not yet create R1 live roots, seeds, manifest, release, result, Cognee scope, or
GPU process.
