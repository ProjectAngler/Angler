# LEARNING status

Freshness: 2026-08-29
Design: draft  
Delivery: not_started  
Current gate: `ANG-GATE-LEARNING-DESIGN-001`

Completed: Tier-1 learning sequence, child map, boundaries, and update contract drafted.  
Blocker: input contracts and first scientific gate are not approved.  
Next: expand FEEDBACK-UPDATE only; keep meta-learning, consolidation, and lifelong features deferred.  
Evidence: no normal-gate evidence; bounded experimental evidence below.
Rollback: remove branch package; Tier-0 blueprint remains unchanged.

Experimental successor: the failed prequential SRWM candidate is preserved,
and `ANG-WORK-LEARNING-BIDIRECTIONAL-PROCEDURE-001` is complete.  Its learned
forward/backward dynamics produced independently executed procedures on 20/20
unique held-out cases.  Equal-budget forward-only also reached 20/20 but used
77.2 mean expansions versus 41.4 bidirectionally; causal controls were
untrained 0/20, corrupted-backward 11/20, and permuted learned guidance 7/20.
Frozen Qwen proposed correct goals on 7/8 semantic cases and Angler reached
every proposed goal 8/8.  This is bounded experimental evidence, not a normal
LEARNING gate, an evolving learner, or a milestone pass.

The causal-operator successor now has a clean oracle-free v5 result: 114/120
on one untouched three-domain partition, with 55/60 four-step compositions and
zero proposal-time world execution.  Selection-only Boxes adaptation improved
18/40 to 33/40 while prior Tokens/Files scores were unchanged; Files adaptation
changed the permitted parameters but did not improve its fixed adaptation set.
Skill-local procedural memory and its Glyph-machine composition successor are
now complete as bounded experiments.  The v3.2 resource controller passed its
one-shot sealed Glyph gate: correct/no-trace/wrong-trace/transition-removed
accuracy was 62.5/28.125/28.90625/23.4375% over 16 final mechanisms and eight
queries per mechanism, with zero complete-plan candidates.  This establishes
learned variable-action acquisition and composition in the synthetic world,
not general reasoning or reconstruction.
`ANG-WORK-LEARNING-SOFTWARE-PIPELINE-RECONSTRUCTION-001` is now the active
experimental leaf.  It requires zero exact support/query address overlap and
novel cross-package motif composition while reusing the learned memory/reasoner
interface; the consumed Glyph final cannot be tuned against.  Normal LEARNING
and milestone gates remain unchanged.

Its fixed public relation-credit v5 run produced the first relation-stage pass:
`25/32` supported rows and six of eight supported streams.  Context learning
improved supported valid-set mass from `0.374410` to `0.475952` and strict
top-one from `0.00` to `0.52`, but missed the frozen `0.60`/`0.80` context gate;
joint training therefore did not run.  This localizes the active failure to
learned context selection rather than relation representation.  Development,
final, judge, and resource scaling remain closed pending a fresh bounded
successor.

The fixed v6 successor passed 99 focused and 129 combined frozen tests plus an
independent static audit, then ran once.  Its R40 loss fell from `0.225882` to
`0.114760`, but the fresh panel reached only `22/32` supported rows and five of
eight qualifying streams against fixed `24/32` and six-of-eight requirements.
It therefore stopped before C or J; the new supported-valid-set context learner
has not yet received an empirical trial.  Exact invariants and access denials
held.  This localizes the immediate blocker to relation-stage robustness across
fresh initialization/identities, not to GPU capacity and not yet to the v6 C
mechanism.  Next is one prospectively specified learned robustness successor,
with causal C testing kept distinct from a later fresh-identity robustness
claim; v6 itself will not be rerun.

The fixed successor is v7: one fresh R80/C25/J35 run.  It doubles only R's
unique learned exposure to 640 streams while preserving the model, objectives,
optimizer, panels, thresholds, and access denials.  There is no R40 selection
point, replay, baseline arm, seed retry, or deterministic repair.  A terminal
R80 failure stops; a pass freezes relation and opens the already-defined
set-likelihood C25, followed by J35 only on a context pass.

V7 passed 99 focused and 129 combined frozen tests plus two independent static
audits, then ran once.  Doubling R to 80 updates/640 unique streams reduced
loss from `0.225913` to `0.113038`, but the fixed fresh panel again reached
only `22/32` supported rows and five of eight qualifying streams.  Its exact
per-stream pattern `[4,3,4,4,2,2,3,0]` repeated v6 despite fresh identity and
twice the exposure.  C/J again remained closed.  Exposure alone is therefore
falsified as the next remedy; the active blocker is a structural learned
relation representation or credit-assignment blind spot.  The next successor
must repair that learned structure without replay, seed shopping, threshold
relaxation, identity inputs, or a deterministic selector.

Public-only tensor diagnostics localized the loss to the old flat-cell
relation readout: motif-3/4 candidate twins remained strongly distinct in the
pair encoder, then became nearly collinear after pooling erased row/column
endpoint co-incidence.  V8 is now frozen and implemented as the smallest
structural successor: a learned two-axis anonymous-node readout with separate
row/column attention, diagonal retention, one node-set attention block, and
four learned invariant pooling seeds.  The context tower, pair encoder,
objectives, R80/C25/J35 exposure, optimizer, thresholds, and gates are
unchanged.  Tests prove rename invariance, dynamic node counts, same-cell-
multiset incidence sensitivity, and gradient reachability through every new
stage.  The focused suite passes `101/101`; the combined frozen evaluator and
runner suite passes `131/131`.  Frozen evaluator hashes remain
`45D2282D...D0DFCE44` and `CFB02F8D...86586D3`.  Three independent final code
audits passed, then the one fixed v8 GPU run stopped at R80.  Its loss remained
nearly flat (`0.225910` to `0.225905`) and the disjoint public panel reached
`0/32` supported rows and zero of eight qualifying streams, with margins only
around `1e-6`.  Context and joint stages did not run.  Every covariance,
empty-memory, shared-parameter, and access-denial invariant remained exact.
V8 therefore falsifies wholesale replacement of the live flat readout by this
randomly initialized hierarchy: isolated structural expressivity did not
produce a usable end-to-end learning path.  The immediate task is a fresh-
identity CPU diagnosis of code dispersion and gradient attenuation, followed
by one learned successor that retains the proven global path while adding
node-incidence structure.  Development, final, judge, control streams, v8
reruns, extra exposure, and resource widening remain closed.

Fresh non-protocol diagnostics then proved v8 had entered an exact stationary
collapse: a shared relation code reproduces its `0.2259075` loss and has zero
code/comparator gradient, while the hierarchy retained only about one seventh
of the legacy twin separation.  V9 is now frozen and implemented as a
function-preserving learned expansion.  It restores the original trainable
cell-attention/mean/max relation path and appends, after every legacy evidence
module, a simpler row/column/diagonal incidence residual with an exactly zero
bias-free output projection.  The fused function is exactly legacy at
initialization; a real public relation loss opens the projection on its first
step and reaches the incidence trunk on the second.  Protocol v9 keeps
R80/C25/J35, all objectives, optimizer settings, thresholds, panels, and
denials unchanged under fresh disjoint identities.  Focused tests pass
`104/104` and the combined frozen suite passes `134/134`; frozen evaluator
hashes remain unchanged.  Three independent final audits pass, including
bit-exact legacy initialization/function, two-step public-loss residual
opening, cross-version seed freshness, checkpoint identity, ownership, and
forbidden-dependency checks.  The one fixed v9 GPU execution is next.

The one fixed v9 GPU run is complete.  It repaired v8's collapsed training
path: R80 loss fell from `0.225931` to `0.122426` and the fresh panel reached
`24/32` supported rows versus v7's `22/32`.  Coverage was nevertheless
concentrated as `[4,3,4,4,3,2,2,2]`, so only five of the required six streams
qualified; effective stream count contracted from about `8.00` to `4.18`.
The unchanged relation gate therefore stopped execution before context or
joint learning.  Exact invariants and access denials held.  V9 will not be
rerun or rescued by a threshold change.  The immediate bounded task is to
diagnose public credit concentration and incidence-residual specialization on
fresh non-protocol identities, then specify the smallest learned successor
that broadens competence without a deterministic selector or replay.

That diagnosis is complete.  The outer entropic objective already emphasizes
hard streams and preserves a positive floor for all eight; the exact mismatch
is the uniform mean over four variations inside each stream.  V10 is the fixed
one-change successor: the v9 model and outer learner are unchanged, while R/J
now use a nested anonymous `0.5` mean plus `0.5` entropic upper-tail row risk at
temperature `0.05`.  Every row keeps at least `12.5%` within-stream gradient,
with extra credit flowing to harder variations.  There are no new parameters,
identities, rules, replay, extra updates, or threshold changes.  V10 uses a
fresh disjoint seed schedule and will run once; complete terminal diagnostics
will be preserved even on a gate stop.  Focused CPU tests pass `105/105` and
the combined frozen evaluator/runner suite passes `135/135`.  GPU execution
remains blocked only until independent byte-level audits finish.

V10 then completed its one in-memory fit and gate evaluation, but the external
artifact harness failed while byte-hashing a scalar tensor before it saved or
printed the returned result.  No report or checkpoint exists, the gate outcome
is unrecoverable, and no learning claim is made.  V10 will not be rerun.  V11
is the fresh recovery identity with exactly the same learned mechanism,
budgets, and gates.  Its only non-learning addition is a tested scalar-safe
model digest, and its fresh train/panel seeds are disjoint from v4 through v10.
Its focused CPU suite passes `106/106` and the combined frozen evaluator/runner
suite passes `136/136`; the new scalar mutation/restore digest test passes.
The one fixed v11 GPU invocation is now complete and independently verified.
R80 loss fell from `0.225905` to `0.119743`, but its fresh panel reached only
`21/32` supported rows and four of eight qualifying streams, distributed
`[4,2,4,4,2,2,3,0]`; the relation gate failed and C25/J35 did not run.  Row
credit remained broad enough to reject simple starvation as the sufficient
explanation.  A post-gate, same-panel incidence intervention improved
continuous witness and separation but changed neither coverage count, then
restored the model bit-exact.  Full WSL report/checkpoint hashes are
`EFDAC946...071AE43` and `2CF650BA...F5F0694`; the bounded workspace record is
`outputs/phase6-software-pipeline-reconstruction-v11-result-summary.json`.
V11 will not be rerun or rescued.  Next is a bounded public-only causal
diagnosis of the recurring hard structural streams, followed by one fresh
learned representation/factorization successor; context, joint, development,
final, judge, and control access remain closed.

The no-update diagnostic
`phase6.public-structure-path-diagnostic.v11-d1` is complete and independently
audited.  Surface rerendering changed no supported row or stream and altered
continuous values only at approximately `1e-7`, while changing topology moved
the same terminal model from `23/32` rows and four qualifying streams to
`29/32` and seven.  The pair tensor retained mean twin L2 near `9.02`; the
global readout compressed it to `0.46--0.50`.  Incidence fusion increased
witness by `0.053--0.054` and changed one support decision per cell, but its
independent projected twin difference was only `0.00030--0.00033`.

The dominant causal lead is shared-update conflict: `53.6--64.3%` of
off-diagonal stream-gradient cosines were negative across every measured
parameter group, reaching `-0.948`, and the pattern reproduced across surface
rerenders.  The checkpoint, competence state, and all parameters remained
exactly unchanged.  Full diagnostic SHA-256 is
`3191E1D9...142BA`; the bounded record is
`outputs/phase6-software-pipeline-reconstruction-v11-d1-summary.json`.

The next bounded construction first isolates one anonymous learned
conflict-aware updater in `phase6.public-conflict-reconcile.single.v12`; its
first relation direction is numerically checked against v11's and subsequent
blockwise weights learn
from symmetric public leave-one-stream-out gradient consequences.  If that
mechanism is sound, the same frozen update interface is used for a
capacity-matched four-cell Angler comparison.  The cluster is a hypothesis,
not an accepted architecture: cells must remain anonymous and independently
plastic, routing/composition must be learned, and gains from extra parameters,
sampling, voting, fixed roles, identity routing, replay, or deterministic
orchestration do not count.  Context, joint, development, final, judge, and
control access remain closed until the relation gate genuinely passes.

The cluster pilot's go rule is intentionally performance-independent: v12 must
complete R80 with valid finite evidence, a live learned mixer, passing fixed
twin/permutation tests, a demonstrably changed post-first controller direction,
and a reloadable combined lineage.  Its relation score
is evidence, not a selector for whether modularity is tested.  This prevents a
favorable-only comparison while keeping integrity failures as hard stops.

V12 has now executed once.  It passed the fresh relation gate at `24/32` rows
and `6/8` qualifying streams, after all `79` post-anchor updates used a
causally different learned direction.  It then stopped at the unchanged context
gate (`0.58333` top-one and `0.45652` mass against `0.80`/`0.60`) while retaining
the relation boundary.  Its reloadable checkpoint and full report passed all
mechanistic license conditions, so `CLUSTER_PILOT_LICENSE_V1` is issued.
Summary SHA-256 is
`901E558537531352CB7B9ACC6089C8497342C18379FB0E853A092D8B1D7E5242`.
The next active construction is one fresh, capacity-matched single-core versus
anonymous four-cell causal pilot; v12 will not be rerun or tuned.

That successor is now implemented as
`phase6.public-anonymous-cluster.paired.v13`.  It fixes three paired,
relation-only R80 replicates with identical fresh streams and panels per arm.
The four-cell learned cluster is all-active, permutation invariant, and
capacity matched at `269,031` complete / `65,387` active parameters versus
`269,010` complete / `65,366` active for the monolith; no roles, sharding,
voting, hard routing, replay, solver, or inert
padding is present.  Both arms use five anonymous updater blocks.  Dedicated
tests pass `7/7` and the combined frozen suite passes `156/156`; one CPU
preflight update reached all four cells and the composer.  Runner/test hashes
are `F1045756...EC8B529` / `2E6D844D...B6DF32`.

The fixed V13 GPU comparison is complete and independently audited.  The
all-active cluster produced `134` supported rows / `24` qualifying streams /
`0.0580147` mean target loss against the capacity-matched monolith's
`137` / `27` / `0.0550869`, so the predeclared disposition is
`CLUSTER_HARMFUL` for this implementation.  Cells nevertheless diverged and
learned fusion beat uniform fusion in all three replicates; the failure is
localized to harmonization and plasticity allocation.  All streams were forced
through all cells, cluster gradient conflict exceeded the monolith, and
multiple lesions beat the fused result.  The independently verified report and
checkpoint hashes are `781BAF57...F53C0` and `734FCB36...44BA2`; the bounded
record is
`outputs/phase6-software-pipeline-reconstruction-v13-cluster-summary.json`.

V13 will not be rerun or post-hoc tuned.  The next active construction is one
fresh anonymous, permutation-equivariant counterfactual plasticity router that
learns both cell read and update allocation from public outcome consequences.
It must remove compulsory per-stream updates without introducing fixed roles,
identity routing, hard selection, voting, gradient surgery, replay, or a
deterministic solver.  Context, joint, development, final, judge, and control
access remain closed until the learned relation mechanism earns advancement.

V14 is now the fixed active experiment.  It uses three fresh paired R80
replicates of byte-identical four-cell clusters: a uniform-plasticity sham and a
learned-plasticity arm.  The existing read composer remains unchanged and the
old conflict mixer is removed from both arms, isolating learned cell-write
allocation.  Cell-local public gradients are computed independently of read
preference.  The anonymous router is trained by the actual held-out public loss
after a differentiable functional SGD cell update across all eight symmetric
leave-one-stream-out folds; no evaluator result, task/cell identity, Hessian,
fixed role, hard router, replay, vote, or solver is available.  The implementation
is isolated in a successor runner/test pair so V12/V13 bytes and evidence remain
unchanged.  CPU contract tests and independent review precede one fixed GPU
execution.

V14 is complete and independently audited.  Its exact report/checkpoint hashes
are `83F4730...AB378` / `5B64D851...1D552`; the read-only audit passed
`111,688` checks after strict CPU reload.  The frozen formal result is
`PLASTICITY_ROUTER_SUPPORTED`, but the project interpretation is
`FORMAL_THRESHOLD_PASS_PRACTICAL_EFFECT_NOT_ESTABLISHED`: learned write routing
improved mean loss by only `2.2662e-8` (`0.116` ppm), both arms remained at
`0` supported rows / `0` qualifying streams, and allocation stayed effectively
uniform.  This result is preserved but is not advancement evidence.  The
bounded record is
`outputs/phase6-software-pipeline-reconstruction-v14-plasticity-summary.json`.

The next active hypothesis is anonymous episodic procedural slotting: restore
competent AdamW cell updates, make the virtual counterfactual exactly match the
actual optimizer state transition, expose only anonymous content/state-derived
competence traces, and train causal allocation across fresh paired surface
variations.  It remains relation-only and local/synthetic.  Advancement now
requires nonzero absolute competence and fixed nonzero effect margins; zero
versus zero and numerical-scale loss ordering are explicitly insufficient.

That hypothesis is now frozen as
`phase6.public-anonymous-cross-variation-plasticity.paired.v15`.  Its balanced
80-update schedule removes V14's procedure co-occurrence and seed-slot alias
risk; lane-local A-to-B/B-to-A meta-learning uses the exact AdamW transition
used by the real cells.  The paired uniform arm retains equal meta-compute but
does not apply its router.  A no-update adaptation control, majority-replicate
lesion checks, lane/stream/cell symmetries, nonuniform routing margins, and
nonzero absolute competence floors prevent numerical or merely state-changing
false positives.  Functional AdamW virtualization has independently matched
ordinary AdamW to one FP32 ULP on all four real cells and propagated held-out
loss gradients to the router without controller mutation.  The isolated V15
runner is now frozen at SHA-256
`C748329ED35055F80EB8859C3A22CDE9D40D59D6FA780766A162EB134711234B`;
its test is frozen at SHA-256
`D2560CC62D5C2031A35BE1CF951E14167CBE789AA8BACF03C86535622C40AA4E`.
The corrected homogeneous anonymous cell lift and FP64-only anonymous
reduction accumulators passed the nine-test focused CPU suite, including a
stateful real-cell/composer/router/optimizer permutation twin.  The measured
worst committed learned-state permutation delta was approximately `1.5e-8`
against the fixed `1e-6` ceiling.  The hidden-identity exclusion test now
covers the full transitive learned closure.  The combined inherited V13--V15
preflight passes `173/173` tests with one expected CUDA-hidden skip.  The active
next action is exact environment/output/hash verification followed by the one
frozen V15 GPU invocation; no V15 adaptive result has yet been observed.

The one V15 invocation is now preserved as
`INVALID_NO_CLAIM / PARTIAL_PRECONDITION_ABORT`.  After approximately 43
minutes it stopped at the update-zero structural warm-up guard before a
terminal evaluation, checkpoint, or report was written.  The failing arm and
predicate were not emitted, so no competence, routing, adaptation, or
harmonization result exists.  A bounded no-update CPU reconstruction found all
three frozen starts finite, exactly uniform, and scorer-reachable.  A separate
scalar VJP check then reproduced the implementation defect: the first-step
functional AdamW square-root backward is nonfinite at an exactly zero direction
on both CPU and CUDA even though its forward update is finite.  The bounded
failure record is
`outputs/phase6-software-pipeline-reconstruction-v15-plasticity-summary.json`.
Do not rerun the V15 identity.  The active next action is a fresh-seed successor
that makes this analytically finite at zero, enforces gradient finiteness before
every update, records structural diagnostics without using them as competence
evidence, and retains every scientific outcome threshold unchanged.

V16 completed its one frozen six-arm run after the exact-zero AdamW repair, but
the formal result is `INVALID_NO_CLAIM`.  Absolute learned competence was
present (`146/192` rows, `28/48` streams), yet five of six surface rerenders
exceeded the frozen `1e-6` ceiling, routing stayed effectively uniform, only
one replicate beat uniform, and paired advantage, fresh adaptation, routing,
and harmonization all failed.  Strict report/checkpoint reload passed with no
lineage discrepancy.  The bounded record is
`outputs/phase6-software-pipeline-reconstruction-v16-plasticity-summary.json`.
V16 is consumed and will not be rerun or promoted.

V12 is retained as the champion.  Its exact remaining deficit is context read,
not relation: relation passed `24/32` and `6/8`, while context achieved only
`0.58333` valid-set top-one and `0.45652` valid-set mass.  The active successor
V17 has now completed once as `INVALID_NO_CLAIM`.  All intended new tensors
trained and strict lineage/reload checks passed, but residual-on gained zero
top-one rows and only `0.000682` valid mass, learned composition was effectively
uniform, and no expert lesion had a top-one effect.  V17 is consumed and is not
a V12 upgrade.  Its bounded evidence is
`outputs/phase6-software-pipeline-reconstruction-v17-context-residual-summary.json`.

The corrected bottleneck is upstream: V12's flat context pool discards anonymous
row/column endpoint incidence, while its old likelihood mostly rewards null
suppression rather than real-slot ranking.  The active successor is
`phase6.public-v12-champion-context-incidence.v18`: reuse the proven V9
`RelationAxisSetReadout` as an exact-zero pre-pool context residual and train
only its 10 tensors / 16,992 parameters for a fixed 256 updates over 2,048
unique streams.  Its supported-row objective is real-slot-normalized set
likelihood with separately bounded presence/abstention terms.  Four fresh
panels and a projection-zero lesion must show recurring unique-valid ranking
and real-normalized-mass gains; null reduction or all-valid rows cannot count.
SRWM remains deferred until this read path is genuinely discriminating.  Next
action: finish the isolated V18 runner/tests, freeze source hashes and panel
identities, independently audit, then execute its one fixed run.

V18 has now completed once as `CONTEXT_INCIDENCE_NOT_SUPPORTED`.  Its 256
updates reached and changed all ten new tensors while preserving exact V12
lineage, but real-slot rank stayed at chance (`24/72` unique-valid), causal
top-one was `-1`, and causal real-normalized mass was only `+0.0000063`.
The branch learned a common-mode null compromise, became effectively rank one,
and erased V12 context behavior (`0/91` live full top-one versus `43/91` under
the same-checkpoint zero-residual lesion).  The independently audited bounded
record is
`outputs/phase6-software-pipeline-reconstruction-v18-context-incidence-summary.json`.

A public-only diagnostic found the required relation structurally recoverable
in all `128/128` frozen-panel graphs with zero anonymous-topology signature
collisions; missing input is not the blocker.  The active successor is
`phase6.public-v12-champion-paired-graph-context.v19`: restore exact V12 and
adapt DeepMind GMN-style bidirectional cross-graph attention so query and stored
graphs interact before pooling.  Its zero-initialized direct real-slot residual
uses temperature-aware log-partition centering, mathematically preserving V12's
null probability and total real mass while allowing real choices to reorder.
Raw anonymous graphs are retained in bounded online trace state and the same
matcher must affect ordinary production `score_actions` and training credit.
The sealed V19 fit completed all 512 updates and preserved terminal checkpoint
SHA-256 `10BB6BAC9BD83F7F4EE0ABF2846CE4133D2133790C2B55113C9044930D2EBC7F`,
but its first `zero_residual` evaluation stopped on a one-FP32-ULP duplicate-row
position effect.  That original outcome remains immutable as
`HARNESS_ERROR_PRESERVED` (`C297B861...116EE`).  The separately authorized
evaluation-only recovery added exact duplicate-row projection only around the
frozen evaluator, ran zero fit or optimizer calls, and accepted its first result
without tuning.  Its terminal report is SHA-256
`55592E9861EC16301603D0CD7BB2A104E596BAAA97BDC65D50DCC517951A0800`.

V19 is `PAIRED_GRAPH_COMPONENT_SUPPORTED`, not a full V12 replacement.  On
four fresh panels, learned versus zero-residual context selection improved
unique-valid top-one from `22/72` to `63/72`, real-normalized valid mass from
`0.35897` to `0.66750`, and informative margin by `+1.79234`; all four panels
showed positive recurrence and no regression, and the frozen relation path was
exact under the primary lesion.  This demonstrates generalization across fresh
topology/surface instances of the same eight mechanisms only; it does not show
cross-mechanism transfer.

The honest supported mechanism is a **learned paired whole-graph context
comparison**.  GMN-specific correspondence is not established: replacing
cross-graph attention with uniform attention preserved `63/63` learned top-one
decisions and essentially identical mass/margin, so a whole-graph-statistic
shortcut is not excluded.  Removing the mismatch message reduced confidence
substantially but retained `62/63` decisions.  Full replacement also remains
blocked by the unchanged V12 relation path (`92/128` supported rows and `18/32`
qualifying streams, below `96/128` and `24/32`), despite both context-quality
thresholds passing.  CODEX and Claude independently recomputed these verdicts
from primitive panel metrics.

Next action: retain the V19 whole-graph comparator as a supported component and
perform the already-approved, read-only representation-overlap diagnostic on
the recurring hard streams before proposing a relation-learning successor.  Do
not open C25/J35, claim GMN attribution, add propagation rounds, or tune V19
from this result alone.

The V11-D2 no-update representation diagnostic has now completed once as
`REPRESENTATION_OVERLAP_INTERFERENCE_NOT_SUPPORTED`; report SHA-256 is
`69D56232A4E70720AFD8428208A0F5ED4B4C2C75AED3D71DFF678F5BA10E6C9F`.
This does not erase V11-D1 gradient conflict.  It specifically finds no
easy-versus-hard concentration in comparator-hidden representation overlap:
E-H overlap was about `0.94699`, below E-E `0.99344`, and zero of four gradient
groups were significant in either deterministic t0 rerender.  The same-module
shared-comparator burden was positive but had exact `p=0.27339`.  Model and
evidence digests remained exact with zero gradients, updates, or checkpoint
writes.  Consequently `d2_same_module_overlap=false` for the pre-registered
ANML trigger.

The active learned successor is V20 OML: retain V19's supported paired
whole-graph context comparison and meta-learn the inherited relation
representation for low-interference online acquisition across fresh and unseen
mechanisms.  Its bounded leaf freezes a second-order OML arm against a
detached-inner-gradient first-order arm, original/meta-seen/heldout evaluation
families, no replay, and first-result/no-tuning evidence.  ANML remains
conditional and cannot be authorized by V11-D2; it requires all separately
frozen V20 trigger operands.  Next action is final leaf audit, isolated
implementation/tests, one synthetic CUDA preflight, and then the one frozen GPU
pilot if every preflight passes.
