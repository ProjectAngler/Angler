# ANG-WORK-LEARNING-CONTENT-ADDRESSED-CAUSAL-MEMORY-V7-001

Status: specified; active learning frontier after frozen V6 development failure

Active node: `ANG-BP-LEARNING` / bounded content-addressed plastic memory

Assurance: LOW; local synthetic software episodes, frozen foundation, no
deployment or external effects

## Evidence-driven need

V6 is preserved `DEVELOPMENT_NOT_SUPPORTED`, result SHA-256
`9BB2015DEAE427518D814398EE2B13205378A5A86917242F3CF7B0F1E6F12DD1`.
It solved 3/8 development mechanisms and tied sign-deranged, zeroed,
memory-disabled, fair-semantic, and Moving-Origin-removed controls on success
count. The true-minus-deranged target probability was -0.00082. Read-only
training diagnosis found true and sign-deranged both 45/64 with only 0.0047
target-probability separation, while zeroed scored 11/64. The single additive
state norm grew to 77.10 over 64 mechanisms and to 9.74 over eight development
mechanisms. Causal gradients reached outcome embeddings and the ANML
neuromodulator, but the memory geometry learned that feedback existed without
preserving its polarity.

## Accountable outcome

Replace the commutative unbounded vector with a bounded content-addressed
plastic memory. Chronological objective outcomes must write distinct learned
procedural evidence into multiple slots; fresh candidate queries must read the
relevant slots. Sign-deranged and zeroed histories must not be behaviorally
equivalent to true history, and state magnitude must remain bounded by design
without replaying prior histories on each update.

## Donor structure and non-cheating boundary

Borrow the established Neural Turing Machine / Differentiable Neural Computer
pattern of learned content addressing, bounded erase/add writes, and learned
reads. Retain OML-style differentiable inner updates and outer held-out losses,
the ANML learned outcome/temporal modulator, frozen Qwen semantics, and Moving
Origin coordinates. Deterministic code may shape tensors, execute the frozen
schedule, and measure outcomes. It may not assign outcome signs, pick slots by
label, map task families to slots, filter candidates, encode generator IDs or
targets, or solve a procedure.

## Learned state and update

- Exactly 16 slots of width 32, with bounded key, value, and usage tensors.
- The learned writer receives semantic pair features, a learned -1/0/+1 token,
  relative temporal features, and read/state context. It emits content keys,
  bounded candidate values, erase gates, and write strength through shared
  networks for all outcomes.
- Content similarity and learned allocation produce differentiable slot
  weights. Convex/erase-add updates keep every state value finite and bounded;
  no additive global drift is permitted.
- A fresh candidate produces a learned read key, attends over slots, and uses
  the read value plus semantic interaction to produce its residual score.
- Functional updates remain differentiable for meta-training and detachable
  for deployment. Foundation observations never retain gradients.

## Data, compute, and scope

Reuse the sealed V6 corpus API: 64 train mechanisms across 16 topology
families, eight already observed development mechanisms across four unseen
families, and eight still-sealed final mechanisms across four further unseen
families. V7 may use train/development only until its own frozen gate passes.
Outcome diagnostic prose and absolute ordinals remain excluded.

Fresh implementation scope is this leaf, one new core and focused test,
reasoning exports, one new runner and focused test, append-only
`AGENTS_SYNC.md`, and fresh remote V7 checkpoint/results. V5/V6 code,
checkpoints/results, V6 final data, Qwen, public remote, and owner workspaces are
not writable.

Use one frozen 256-pair/64-step meta-training pass with the V6 true,
sign-deranged, and zeroed causal objective. Record slot attention entropy,
write/read concentration, slot/value bounds, state norm, causal gradients, and
all V6 removal controls. No seed retry, threshold change, or result tuning.

## Frozen development and final gates

The V6 gates remain unchanged: full at least 6/8 and at least two successes
above state-reset, fair-semantic, deranged, zeroed, memory-disabled, and
Moving-Origin-removed; mean target advantage and failed-mass reduction at
least 0.10 over both outcome counterfactuals; at least one failed-to-pass
repair; equivalent-family state swap at least full successes and 90% target
probability retention; sequential retention at least 0.90; state keys/values
within declared bounds; aggregate effective write usage of at least four slots
with no slot receiving more than 0.50 of aggregate write mass; and exact frozen
Qwen/core identities during evaluation. Passing development alone opens one
final run with the same gate.

Passing is bounded synthetic evidence for content-addressed outcome-causal
procedural apprenticeship, not arbitrary reasoning, AGI, consciousness,
deployment safety, or production readiness.

## Stop and rollback

Stop on leakage, non-finite state/gradient, bound violation, final access before
development pass, counterfactual inequality beyond outcome tokens, result
reuse, foundation mutation, deterministic solution logic, or evidence failure.
Preserve all terminal results; roll back only fresh V7 artifacts.
