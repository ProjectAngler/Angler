# ANG-WORK-LEARNING-CAUSAL-NEUROMODULATED-APPRENTICESHIP-V6-001

Status: specified; active learning frontier after the frozen V5 development failure

Active node: `ANG-BP-LEARNING` / outcome-causal meta-updater successor

Assurance: LOW; local synthetic software episodes, frozen foundation, no
deployment or external effects

## Evidence-driven need

V5 is preserved `DEVELOPMENT_NOT_SUPPORTED` at SHA-256
`4B75A31212C7EE4C0A0A7893D8F5DCDEE587DB77A74F3BAFF43EDE06B4D70FCF`.
Full OML solved 2/8 development mechanisms and tied outcome-shuffled and
Moving-Origin-removed controls. A read-only checkpoint diagnostic on the 32
training mechanisms found full 24/32, outcome-shuffled 24/32, and
outcome-zeroed 29/32. Loss decreased from 1.2678 to 0.3505, so optimization
worked while the learned update bypassed outcome causality and overfit the four
training procedure families. The V5 final partition remains unopened.

## Accountable outcome

Train a procedural learner whose behavioral change is causally attributable to
objective success/failure observations. The same chronological trace stream
with true outcomes must outperform counterfactual label derangement and
outcome removal. An ANML-style learned neuromodulator gates what enters the
constant-size plastic state; code supplies no sign rule, procedure answer, or
success filter.

## Data and leakage boundary

- Expand training to at least 64 mechanisms across at least 12 distinct
  procedure topologies. Development and final each contain exactly eight
  generator-family-disjoint mechanisms.
- Every stream contains balanced interleaved success/failure experience, a
  fresh structural challenge, and declared Moving-Origin coordinates.
- Qwen remains a frozen detached semantic encoder. Generator identity,
  topology labels, targets, seeds, and expected procedure indices never become
  learner features.
- Counterfactual outcome streams reuse identical public text, ordering,
  embeddings, and coordinates. Only the observed outcome tokens differ.

## Intended learned mechanism

Reuse the V5 OML semantic representation and functional state, adding one
learned neuromodulator that receives semantic, outcome-token, temporal, and
current-state context and emits bounded feature/write gates. Meta-training
optimizes held-out usefulness, pairwise preference, anti-repetition, retention,
bounded state, and an explicit outcome-causal contrast:

1. true-outcome target probability must exceed deranged and zeroed streams;
2. true-outcome failed-procedure mass must be lower than both counterfactuals;
3. gradients from this contrast must reach the learned outcome embedding and
   neuromodulator;
4. no hard-coded positive/negative sign, outcome mask, candidate reorder, or
   task-specific procedure rule is permitted.

## Literal implementation scope

- this leaf;
- one fresh V6 corpus module and focused corpus test;
- one fresh learned-core module under `src/angler/reasoning/`, reasoning
  exports, and one focused core test;
- one fresh V6 train/development/final runner and focused runner test;
- append-only `AGENTS_SYNC.md`;
- fresh remote checkpoints/results under a
  `causal-neuromodulated-apprenticeship-v6` identity.

V5 code/checkpoint/result/final data, prior evidence, public repository, Qwen
weights, live owner workspace, external services, and promoted state are not
writable.

## Frozen development and final gates

Development opens final only if, across exactly eight development mechanisms:

- full solves at least 6/8;
- full solves at least two more than state-reset, fair-semantic,
  outcome-deranged, outcome-zeroed, memory-disabled, and
  Moving-Origin-removed controls;
- true outcomes increase target probability and reduce relevant-failure mass
  by at least 0.10 relative to both outcome counterfactuals;
- at least one initially failed challenge becomes a pass;
- equivalent-family state swap retains at least 90% target probability and no
  fewer successes than full;
- sequential retained target probability is at least 0.90 of immediate access;
- Qwen and the trained core are byte-exact before/after evaluation.

The same thresholds apply once to exactly eight final mechanisms. First valid
development and final results are accepted without seed retry, threshold
change, or post-result tuning. Passing is evidence for bounded synthetic
outcome-causal procedural apprenticeship, not arbitrary reasoning, AGI,
consciousness, deployment safety, or production readiness.

## Stop and rollback

Stop on leakage, non-finite state/gradient, hidden-final access before the
development gate, counterfactual input inequality beyond outcome tokens,
foundation mutation, result reuse, deterministic solution logic, or evidence
failure. Preserve every terminal result and roll back only fresh V6 artifacts.

