# ANG-WORK-LEARNING-SITUATED-MEMORY-V2-001

Status: consumed; `SUPPORTED_FOR_PROTOTYPE`; V1 remains immutable

Assurance: bounded local synthetic research; no promotion authority

## V1 diagnosis fixed by this successor

V1 classified `NOT_SUPPORTED`: full accuracy was 0.47802734375,
frozen-origin accuracy 0.46923828125, and target removal caused no reduction.
The sealed no-update diagnostic found mean attention on the nominated current
success of approximately 1.25e-15 and top-attention frequency 0.0. The reader
had learned an unintended exclusion shortcut because all four actions appeared
as successes and absolute acquisition ordinal remained learner-visible.

V2 changes only the evidence construction and learner-visible coordinate
surface needed to remove those confounds:

- acquisition ordinal remains trusted provenance but is withheld from the
  learned feature vector; live self-relative age remains visible;
- each query receives one current success, one older success, and one failure
  for every action, so the answer cannot be inferred as the missing member of
  an exhaustive historical-success set;
- the retrieval-removal arm substitutes a different query's complete
  candidate set instead of deleting a redundant single row;
- success/failure wording is balanced and repeated near the action token so a
  frozen Qwen representation can carry both operands.

All V1 thresholds, counts, CUDA requirements, held-family split, causal arms,
nonclaims, and first-result/no-tuning rule remain unchanged. V2 is not a retry
of V1; it tests the intended computation after removing demonstrated
measurement confounds.

## First accepted result

The unchanged V2 thresholds passed on the first result:

- full accuracy: 0.9951171875;
- held-family accuracy: 0.9920477137176938;
- frozen origin: 0.47314453125;
- coordinates removed: 0.4736328125;
- content removed: 0.2470703125;
- target/retrieval removed: 0.0009765625;
- competence reset: 0.2646484375;
- maintained versus fair-naive inspection: 6 versus 42 records.

The neural path ran on an RTX 5080 with PyTorch 2.13.0+cu130. Result SHA-256:
`2d726321c3d8136cecd8bebba5cfa611d18d565587fa0cc0f93614c89124207f`;
checkpoint SHA-256:
`b94e27ad0a42e3f499b7de5ff1b5217463dadd38e60934a7af7f3ebd2da4e3d4`.

The separate live self-hosted Cognee clause also passed. Cognee 1.5.3
retrieved six validated projections for every one of eight queries with target
coverage 1.0. The sealed reader then achieved 0.875 action accuracy without
training, with mean target attention 0.8751317175920121. Live recall SHA-256:
`c59083344f1accd8d44bd002922138d26628f4e42895c806d61f360185e8dd45`;
live evaluation SHA-256:
`22fc22b839a1de2629192da5d7272a7020ac1e2f97f30d63a0080ba476054b43`.

This establishes a bounded learned composition across fresh instances of the
same eight synthetic mechanisms. It does not establish arbitrary
cross-mechanism transfer, graph reasoning, AGI, or production readiness.
