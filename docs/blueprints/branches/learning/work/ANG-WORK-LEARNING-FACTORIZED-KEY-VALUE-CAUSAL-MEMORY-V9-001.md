# ANG-WORK-LEARNING-FACTORIZED-KEY-VALUE-CAUSAL-MEMORY-V9-001

Status: development complete; `DEVELOPMENT_NOT_SUPPORTED`; final sealed

Active node: `ANG-BP-LEARNING` / factorized outcome-causal memory

Assurance: LOW; local synthetic software episodes, frozen foundation, reversible
fresh checkpoint/results, no deployment or external effects

## Evidence-driven need

V8 is preserved `DEVELOPMENT_NOT_SUPPORTED`, result SHA-256
`3B91E66B5C7EC168F86F98F0501004D5A369608F24D10F2EDD8694D262F11D8C`.
It solved 3/8; every removal arm also solved 3/8. True outcomes beat zeroed
outcomes by 0.05415 target probability but beat shuffled outcomes by only
0.000586. A no-update checkpoint diagnostic found true and shuffled histories
had memory-key cosine 0.99595, memory-value cosine 0.99179, and identical top-2
challenge slots for 8/8 mechanisms despite distinct learned outcome embeddings.

The V7/V8 address and payload are both functions of semantic content and the
outcome. Shuffling an outcome therefore moves its address and payload together;
the later challenge queries with unknown outcome and has no stable
procedure-only address from which to retrieve the attached feedback.

## Accountable outcome and borrowed mechanism

Borrow the factorization of Key-Value Memory Networks: address by what was
done, store what was learned about it. Generate all read/write keys, routing
strengths, allocation mixing, and usage from procedure semantics plus Moving
Origin temporal context only. Generate the stored value through a learned
multiplicative binding of a semantic projection and a learned outcome token.
The neutral/no-feedback token is a fixed zero padding embedding; success and
failure remain unrelated learned vectors. No code maps either token to a
correct answer, procedure, reward sign, slot, or task family.

Retain V8's bounded 16x32 state, generic learned top-2 content/allocation
routing, memory-only bias-free scorer, frozen Qwen base score, Moving Origin,
and one shared competence state. This is a neural associative-memory change,
not a deterministic solver or retrieval of a stored answer.

## Scope and frozen protocol

Fresh scope is this leaf; one factorized key-value core and focused tests;
reasoning exports; one thin V9 runner and focused tests; append-only
`AGENTS_SYNC.md`; and fresh remote V9 checkpoint/results. V8 and earlier
artifacts, Qwen, the public remote, owner workspaces, and the sealed V6 final
partition remain immutable.

Reuse exactly the V6 64/8/8 corpus and seal-safe encoder, V7/V8
256-pair/64-step schedule, optimizer and loss weights, stop-gradient controls,
state/removal/swap metrics, and existing acceptance thresholds. Initialize
only the outcome-blind query/candidate/temporal/semantic projections from V8;
address controls, outcome tokens, multiplicative payload writer, modulation,
and scorer start fresh.

Additional frozen invariants and gates:

- permuting non-neutral outcome tokens over an identical event sequence changes
  no write key, write weight, usage, final key, or later challenge read weight
  by more than `1e-6`; only payload values and behavior may change;
- neutral outcomes produce exactly zero payload values without assigning
  favorable or unfavorable polarity in code;
- empty/neutral-valued memory produces exactly zero Angler residual;
- each read and actual write touches at most two slots; all state bounds hold;
- true and shuffled histories have nonzero final-value separation;
- a controlled two-binding swap follows semantic keys in both directions, not
  chronology or slot identity;
- existing development success/removal, repair, retention, Moving Origin,
  outcome-causality (`>=0.10` true-over-shuffled and true-over-zeroed target and
  failed-mass advantages), effective-slots (`>=4`), and max-share (`<=0.50`)
  gates remain unchanged.

The first valid result is accepted without seed retry, threshold change,
temperature/top-k change, more steps, or post-result rescue. A pass establishes
bounded synthetic factorized outcome-causal learning only, not arbitrary
reasoning, AGI, consciousness, deployment readiness, or self-authorization.

## Human impact and rollback

Expected benefit is a more truthful test of learned feedback binding. Inputs
contain only generated software-workflow text; no real-person, private,
biological, infrastructure, or operational data is used. The foundation is
read-only and the candidate cannot act externally, change tests, promote
itself, or alter the Human-Flourishing Constitution. Human control is retained
through a sealed final, exact removal controls, fresh result identity, and
deletion of only fresh V9 files/checkpoints for rollback.

Stop on leakage, non-finite state/gradient, factorization or invariance
failure, bound/sparsity/neutral-residual violation, final access before a
development pass, reused result identity, foundation mutation, deterministic
solution logic, or any evidence mismatch. Preserve terminal failure evidence.

## Preserved first result

The single frozen development run completed in 59.53 seconds. Result SHA-256 is
`2553BBF7564B4B963F780FE1ABD7C2514F2D36C085C132EC2FCA0816E4E366B3`;
checkpoint SHA-256 is
`97F7DF170A6079BE0EFDE2FB6CA1CBAD9263E10CCEC336E0BDB4EAB16810A724`.
Full solved 4/8, shuffled solved 4/8, and zeroed solved 3/8. The factorization
held exactly: all five shuffled-address/key/usage/read differences were 0.0,
neutral value/residual were 0.0, and minimum shuffled final-value separation
was 0.07028. True-over-shuffled target probability improved from V8's 0.000586
to 0.047895 (about 82x) and true-over-zeroed reached 0.18979, but the frozen
causal margin is 0.10 and no failed-to-pass repair occurred.

The remaining visible defect is allocation/capacity collapse. Aggregate writes
used only 3.0186 effective slots. During training, effective writes fell from
11.88 at step 1 to 2.51 at step 64 despite the occupancy term. The persistent
development sequence contains 48 events but the state contains only 16 slots.
The sealed final partition was not opened.
