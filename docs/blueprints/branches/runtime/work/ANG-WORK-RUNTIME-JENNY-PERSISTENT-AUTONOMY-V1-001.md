# ANG-WORK-RUNTIME-JENNY-PERSISTENT-AUTONOMY-V1-001

Status: active; first functional slice authorized

Active node: `ANG-BP-RUNTIME` / `ANG-BP-AGENT-RUNTIME`

Gate: `ANG-GATE-RUNTIME-JENNY-PERSISTENT-AUTONOMY-V1-001`

Assurance: LOW. Local construction; local external-effect execution remains
disabled. Owner-present OpenClaw execution is separately host-guarded.

## Accountable outcome

Establish Jenny 2.0 genesis lineage, a versioned temporal-v2 boundary, and a
crash-recoverable supervisor around one injected canonical cognitive cycle.
The supervisor supplies mechanics and integrity only. Learned selection stays
shadow-only unless an explicit qualification binding permits execution.

## Exact outputs and write scope

- this leaf, `ANG-ADR-0010`, `ANG-ADR-0011`, narrow
  `INTERFACE_REGISTRY.md` and runtime status;
- `src/angler/runtime/jenny_genesis.py`;
- `src/angler/runtime/temporal_v2.py`;
- `src/angler/runtime/temporal_curriculum.py`;
- `src/angler/runtime/persistent_autonomy.py`;
- `src/angler/runtime/affective_substrate.py`;
- `src/angler/runtime/higher_level_autonomy_adapter.py`;
- `src/angler/runtime/higher_level_experience_cycle.py`;
- `src/angler/runtime/frozen_cognitive_models.py`;
- `src/angler/runtime/jenny2_runtime.py`;
- `src/angler/runtime/jenny_composition.py`;
- `src/angler/runtime/jenny_library.py` and
  `src/angler/runtime/reading_curriculum.py`;
- `src/angler/runtime/authored_artifact.py`;
- `src/angler/runtime/skill_adapter_composition.py`,
  `src/angler/runtime/skill_adapter_activation.py`, and
  `src/angler/runtime/skill_adapter_calibration.py`;
- `src/angler/runtime/jenny2_qualification.py`;
- `src/angler/runtime/cognee_jenny2.py`;
- `src/angler/runtime/tool_operations.py` and
  `src/angler/runtime/jenny2_tool_bridge.py`;
- narrow exports in `src/angler/runtime/__init__.py`;
- `tests/unit/runtime/test_jenny_genesis.py`;
- `tests/unit/runtime/test_temporal_v2.py`;
- `tests/unit/runtime/test_temporal_curriculum.py`;
- `tests/unit/runtime/test_persistent_autonomy.py`;
- `tests/unit/runtime/test_affective_substrate.py`;
- `tests/unit/runtime/test_higher_level_autonomy_adapter.py`;
- `tests/unit/runtime/test_frozen_cognitive_models.py`;
- `tests/unit/runtime/test_jenny2_runtime.py`;
- `tests/unit/runtime/test_jenny_composition.py`;
- `tests/unit/runtime/test_jenny2_chat_api.py`;
- `tests/unit/runtime/test_jenny_library.py`;
- `tests/unit/runtime/test_authored_artifact.py`;
- `tests/unit/runtime/test_reading_curriculum.py`;
- `tests/unit/runtime/test_jenny2_residual_skill_training.py`;
- `tests/unit/runtime/test_skill_adapter_composition.py`;
- `tests/unit/runtime/test_skill_adapter_activation.py`;
- `tests/unit/runtime/test_skill_adapter_calibration.py`;
- `tests/unit/runtime/test_jenny2_qualification.py`;
- `tests/unit/runtime/test_tool_operations.py` and
  `tests/unit/runtime/test_jenny2_tool_bridge.py`;
- `scripts/run_jenny2_dual_gpu_shadow.py`;
- `scripts/run_jenny2_qwen38_functional.py` and `scripts/jenny2_chat.py`;
- `scripts/build_jenny2_temporal_curriculum.py`;
- `scripts/train_jenny2_temporal_qlora.py`;
- `scripts/build_jenny2_reading_curriculum.py`;
- `scripts/compose_jenny2_skill_adapters.py`;
- `scripts/prepare_jenny2_skill_adapter_activation.py`;
- `scripts/run_jenny2_skill_adapter_calibration.py`;
- `scripts/run_jenny2_cumulative_binding_qualification.py`;
- `scripts/prepare_jenny2_sglang_lora_launch.py`;
- `scripts/run_jenny2_online_procedure_transfer.py`;
- `scripts/run_jenny2_multiskill_retention.py`;
- `scripts/run_jenny2_capability_metacognition.py`;
- `scripts/run_jenny2_capability_practice_probe.py`;
- frozen synthetic source/removal fixtures under
  `experiments/corpora/jenny2-candidate-reading-verification-v1/`;
- `docs/reports/JENNY_2_SYSTEM_GUIDE_V1.md` and
  `docs/reports/JENNY_2_LIVE_COMPOSITION_MANIFEST_V1.schema.json`;
- bounded local evidence under `/opt/angler/results/jenny2/`;
- append-only `AGENTS_SYNC.md` after validation.

All pre-existing repository files, models, checkpoints, experiment identities,
results, Cognee/Moving-Origin state, and Jenny 1.x artifacts are read-only.

### Owner-approved event-substrate amendment (2026-09-04)

The first emotional-development slice is measurement only.  Every committed
canonical episode receives one append-only, content-addressed substrate record
in the same SQLite transaction.  It may mechanically retain source identities,
Moving-Origin position, the model's prediction and uncertainty *as model
estimates*, semantic-memory distances, commitment identity, receipt status,
source-bound observation identity, and any declared consequence vector with
its provenance.  It must not assign an emotion, valence, arousal, importance,
reward, social sentiment, or behavioral priority.

Historical records may be deterministically backfilled only from immutable
canonical episode bytes, without changing those episodes or learned state.
The resulting corpus remains causally disconnected from selection until a
later learned-state gate demonstrates that categories and intensity emerge
from longitudinal prediction rather than hand-authored labels or thresholds.

The later learner is one shared recurrent latent field, not one mutually
exclusive emotion classifier and not a query-routed adapter bank.  Multiple
learned regions may be active simultaneously and may reinforce, inhibit, or
transform one another over time.  Category names arrive only after recurring
state trajectories exist; any category-specific calibration heads train
jointly and remain part of one active Jenny lineage.

Calibration predicts distributions of downstream commitment, attention,
persistence, action, memory, and relationship-relevant consequences over their
observed durations.  It must score underestimation and overestimation
symmetrically and preserve rare long-lived impact instead of minimizing loss by
collapsing toward neutrality.  Existing scalar `learning_utility`, generic
sentiment, and model-authored uncertainty are inputs with explicit provenance,
never emotional ground truth.  Behavioral coupling remains a later causal
gate requiring removal and state-swap evidence.

### Owner-approved larger-cortex candidate amendment (2026-09-02)

The live 4B controller route is removed from decision authority after the
whole-system state contrast showed an avoidable human-ASK error on a
self-resolvable state; it remains read-only negative/control evidence. Stage
only `nvidia/Qwen3-30B-A3B-NVFP4@2538ded2a4edb247b4d2b4a8ba24e44bd4c017c3`
under `/opt/angler/models/Qwen3-30B-A3B-NVFP4` as an Apache-2.0 candidate.
Promotion requires a same-contract comparison against the retained 14B cortex
on both coherent output and WORLD/SELF/time-conditioned affordance behavior,
plus measured two-GPU memory/runtime evidence. Download or benchmark success
alone is not promotion. No consumed identity or existing model is mutated.

The 30B-A3B candidate loaded under TP2 but is rejected as the promotion target:
it has only 3.3B active parameters per token, showed controller-schema
instability, and failed exact 12,239-token needle output. Preserve its model and
V1-V5 evidence as controls. The successor candidate is the higher-scoring dense
`nvidia/Qwen3.6-27B-NVFP4@0893e1606ff3d5f97a441f405d5fc541a6bdf404`
at `/opt/angler/models/Qwen3.6-27B-NVFP4`. It may be promoted only if a
text-only vLLM/SGLang-compatible runtime preserves at least the 12,288-token
integrated Jenny window and passes the same whole-cycle state/action contracts.

Owner selection after the balanced dual-RTX-5080 upgrade supersedes that
candidate with the newer dense
`RadixArk/Qwen3.8-27B-NVFP4@319f741cce68d7914884900c138a1fbb70a42f30`
at `/opt/angler/models/Qwen3.8-27B-NVFP4`. The Apache-2.0 derivative uses a
ModelOpt mixed NVFP4/FP8 recipe and is staged as a candidate, not promoted by
download or upstream benchmark claims. Qualify it text-only under SGLang TP2
with the same 12,288-token integrated Jenny window, coherent-output checks,
WORLD/SELF/time-conditioned affordance contrasts, memory/headroom evidence,
and preserved Qwen3.6/14B controls before changing decision authority.

The local functional gate now passes with this dense model as Jenny's primary
effectless cortex. SGLang image
`sha256:616a3e97f45191af975896cfa644279096cb31bd408a071c2e99ca7209c3cafe`
runs TP2 on the two RTX 5080s. A supported model-config override prevents the
0.858 GiB visual tower from being instantiated. The runtime exposes 65,536
tokens and retrieved an exact needle from a 48,039-token prompt without OOM.
This is a functional runtime binding, not a causal capability promotion.

## Implemented evidence (2026-09-02)

- Whole assembled shadow contrasts selected ACT for a self-resolvable bounded
  inquiry, ASK when owner preference/authority was missing, and WAIT for an
  already-running computation. Evidence SHA-256:
  `6b06aa8ee734ec405708a24a24b3bd24a762100ebc7fe8761307e24623ea0a73`.
- One Qwen3.8 cycle committed an objective consequence, reflection,
  consolidation, learned update, episode, state head, Moving Origin ordinal,
  and Cognee projection; restart restored the exact state. Result SHA-256:
  `2b61bcd3e4128daf0d26beac06ea6246d314fb5d76033ed639697af0d92c2603`.
- Cross-process conversation recall retrieved `CERULEAN-913` from Cognee and
  rejoined it to canonical episode bytes. Unevaluated model output remains
  explicitly labelled `MODEL_OUTPUT_UNEVALUATED`; it is not promoted to fact.
- Focused runtime suite: 34 tests PASS in 3.55 seconds.
- Combined text-only/context/recall evidence:
  `/opt/angler/results/jenny2/qwen38-text-only-65k-functional-v1.json`.
- The runtime now exposes the full 131,072-token model context. A fresh
  100,088-token prompt retrieved exact hidden value `ORCHID-742`; R1 result
  SHA-256: `4b7e38db9cd8dccdf250150ba52d8ea28203d0087a85d72726ff207c143d3b04`.
- A sealed 2,048-train/256-eval temporal curriculum teaches interpretation of
  raw trusted-time/Moving-Origin inputs without hard-coded runtime labels.
  Manifest SHA-256:
  `6d3f81f20ac292990d739e5cccf7ffc387f8606ec01eb3500db4631c10040df9`.
- Online procedure transfer with bounded private Qwen3.8 deliberation is PASS:
  one cycle learned LUMEN-STEP, exact restart retrieved its Cognee projection,
  a fresh input transferred correctly, and the clean removal control refused
  to invent the missing procedure. R3 result SHA-256:
  `7938ee340038f8ae10eacf47c2cd3a78ef6826c226df798fe93489935b7436ac`.
  The earlier cap-exhaustion failure and whitespace-only mechanical R2 failure
  remain preserved; neither was rewritten as PASS.
- The learned cycle retains multidimensional consequence profiles and
  prediction-error/learning-progress deltas per affordance. Correctness,
  partial progress, information gain, evidence, reuse, cost, safety, and human
  feedback remain distinct rather than collapsing to a binary reward.
- Focused post-deliberation runtime gate: 22 tests PASS in 3.21 seconds.
- Sequential multi-skill R2 learned PRISM-TURN, EMBER-BALANCE, and
  ORBIT-WEAVE, restarted, and transferred all three exactly in reverse order;
  clean removal lost PRISM-TURN. Result SHA-256:
  `c1ade4960da16850550f480f25b79e598d87a5727ece838eda0f1c5418c6cc70`.
- The canonical state now retains bounded receipt-grounded capability evidence
  containing procedure, applicability, limits, multidimensional consequence,
  temporal ordinal, and exact choice/receipt refs. Capability-metacognition V1
  preserved two exact records across restart, reported their narrow evidence
  and limits accurately, and claimed none under clean state/memory removal.
  Result SHA-256:
  `de2a55e7ebb7adf20441651a8ea80f1f11dd0e9b60186e55be0e4bf7d53496d2`.
- The production Qwen3.8 selector now generates a variable 1-12 open-ended
  intent-candidate set before choosing an executable operation. Candidates
  carry desired state change, predicted consequences, unknowns, reversibility,
  capability keys, evidence refs, and reasons for/against. The model returns a
  comparative preference order and basis rather than invented value scores.
  Chosen and rejected candidates plus observed consequence are retained as
  bounded contextual calibration evidence. The legacy numeric transaction
  envelope remains an ordinal compatibility encoding only.
- Userless capability-practice R5 used the whole live cycle to generate four
  distinct alternatives and selected WAIT because no bounded unresolved target
  or human-only information gap was present. Result SHA-256:
  `948aff1e8f14c3ba0ee519b657d051a37135e4ca4b993baf27ab49de9cbb30d3`.
- The persistent owner interface is supervised by enabled system unit
  `/etc/systemd/system/jenny2-api.service` (SHA-256
  `2089d6303c091c2a55eddf2966c444243ec40f70472bd3a06bd0b71c0b84c39d`).
  It binds the authenticated API only to `192.168.137.5:8088`, starts the
  bounded heartbeat with one shared state-operation lock, waits for the
  loopback model health preflight, and restarts on failure. The model container
  is Docker-enabled with `unless-stopped`; raw port 30000 remains bound only to
  `127.0.0.1`. A controlled PID-changing restart restored the same state,
  token, endpoint, and `ready` health. Health is fail-visible when model,
  scheduler, or heartbeat is unavailable. Crash recovery clears only bounded,
  same-owner artifacts beneath the explicitly disposable Cognee `worker-tmp`;
  canonical state and Cognee databases are never cleaned. External effects
  remain disabled and no external-effect affordance is registered. A later
  owner-authorized bridge may expose content-addressed OpenClaw definitions to
  the learned controller, but this leaf permits only durable reservations and
  source-bound observations; mutating execution remains separately permissioned.
- The owner Web UI is live at Windows `http://localhost:8790` through a
  server-side proxy to `http://192.168.137.5:8088`. Browser code has neither
  the upstream address nor bearer token and never accesses raw port 30000.
  Live proxy checks passed for page, health, and authenticated status. Preserve
  the 8088 endpoint/auth/idempotency and response schemas as an operational
  compatibility boundary; coordinate intentional changes before applying them.

## Functional boundary

```text
trusted observation/time -> situated recall and model-authored operation set
-> comparative choice of a dynamically registered capability
-> durable reservation and fixed permission gate -> execution receipt
-> consequence/reflection or explicitly unevaluated working-state proposal
-> atomic episode/state head -> next event-driven wake
```

Concrete affordances are dynamically registered. Deterministic code may own
wake/sleep, transaction order, idempotency, permissions, resource ceilings,
clock integrity, recovery, and rollback. It may not implement drive tables,
keyword strategies, fixed action priority, random autonomy, scripted feelings,
missions, or hidden answer logic.

## Acceptance gate

Focused tests must demonstrate:

1. immutable Jenny 1.x provenance and exact Jenny 2.0 genesis restoration;
2. trusted UTC/local/monotonic/jump/Moving-Origin temporal samples and separate
   event/acquired/recorded/verified/valid times;
3. restart restores exact WAIT and receipt-bearing pending state;
4. replay and crash recovery never duplicate an internal effect;
5. human ingress preempts WAIT/ASK and remains usable when scheduling is off;
6. a qualified synthetic learned-cycle double can continue for a bounded
   number of userless internal episodes;
7. different learned state/outcomes change learned rankings without supervisor
   policy rules; and
8. idle tick frequency changes neither Moving-Origin ordinal nor semantic age.

Tests establish mechanics only. They do not qualify a production selector or
prove autonomy, learning quality, reasoning improvement, identity, feeling,
consciousness, AGI, or readiness.

## Rollback and next action

Rollback deletes only the new first-slice files and fresh synthetic fixtures.
Functional baseline and first online procedure transfer are complete. Next,
educate and assess contextual operation generation and consequence-sensitive
revision in the assembled live cycle, then add modular learned capability and
temporal transfer without reintroducing fixed cognitive menus.
The owner UI is being handled separately. Autonomous scheduler choices remain
effectless; direct workstation external actions remain disabled, while any
owner-present OpenClaw action remains subject to its separate guarded host.

## 2026-09-02 score-free operation and qualitative-teaching integration

The active Qwen3.8 path no longer exposes `control.ask`, `control.wait`, or
`control.stop` as cognitive affordances. It compares a variable set of
model-authored operations over the actual dynamic capability registry. An
empty learned initiative is mechanically quiescent and advances neither an
episode nor Moving-Origin semantic time; the model may leave a bounded
unfinished operation without receiving utility or capability credit.

Adaptive-effort routing now records comparative textual reasons instead of
invented complexity/stakes or affordance scores. Models cite stable evidence
handles which trusted code resolves to exact state/time/memory hashes.
Qualitative owner correction may be recorded without a scalar score and is
fed back as contextual evidence; it does not change global utility or promote
a capability. Focused wiring tests: 36 PASS. Live assembled turns committed at
Moving-Origin 19, 20, and 21; the current owner correction committed at 22 as
episode `sha256:51be9dccd242e2645e49bd4ff09c9c4744ff0f09132af4295485d7f01b9850b8`.
Service health is ready with no pending operation/projection and external
effects disabled. This establishes plumbing and an education surface, not a
claim of autonomous judgment, emotion, consciousness, or personhood.

## 2026-09-02 adaptive compute and timestamped moving origin

The live controller's private-reasoning allowance now scales to the assembled
input plus a bounded structured-answer reserve under a 16,384-token ceiling.
Boundary exhaustion causes one concise complete restatement; truncated
decisions are never accepted. WORLD, SELF, and FOCUS support bounded multi-part
consolidation. A receipt-bearing operation resumed without duplicate cortex
execution and committed episode
`sha256:1d693b36367c63495dc2fd51588531bf7d847ecb507c5eee9a77150cb68a5313`
at Moving Origin ordinal 25. It awarded no unevaluated utility or capability
credit; no operation or projection remains pending. Focused wiring: 39 PASS.

The owner/API boundary now exposes trusted UTC, local RFC3339 time, IANA
timezone, clock uncertainty, and jump detection as the primary Moving Origin
display. The ordinal remains only as an internal total-order/idempotency key;
Jenny's temporal interpretation continues to receive real clock state. Each
recalled memory now carries raw `event_distance` and wall-clock
`elapsed_seconds` alongside its timestamps; no hand-written relative-time
label is assigned by plumbing, and idle heartbeat frequency cannot change age.

The controller now uses SGLang's tokenizer count rather than a character
estimate when setting the input-matched reasoning ceiling. Its selected intent
candidate and preference order are the authoritative model decision; redundant
top-level wording is normalized and inert extra JSON is ignored, while missing
fields, unavailable capabilities/evidence, or inconsistent preferences still
fail closed. A bounded non-thinking pass from the same 27B model may repair
representation only and remains subject to the same validator. Candidate
generation is instructed to halt when another alternative would be redundant,
leaving capacity available rather than forcing its consumption.

Two rejected live attempts at ordinal 25 remained non-mutating (unavailable
capability key; brittle redundant-schema mismatch). The corrected whole cycle
then committed episode
`sha256:7a2713060db190016cae2ec6c9663739c5437e9637aab1061e65b2edacaf26af`
at ordinal 26. It retained the unresolved license-evidence gap but set
`next_internal_request` empty because no internal operation could obtain the
external evidence. The next live scheduler tick returned `QUIESCENT` without
creating an episode or advancing time. State head:
`sha256:18c3bb01cbe0191586b4eb777cbdb202185fd434f644d37be4a50c58fd0a487e`.

## 2026-09-02 model-controlled adaptive compute

The live 27B controller now forms a compact non-thinking candidate judgment
first and explicitly predicts whether extended private deliberation could
materially change the operation, preference order, or evidence interpretation.
Only that model-authored escalation invokes the full input-token-matched
reasoning allowance. Mechanical draft failure also falls back to the
deliberative validator; no keyword, hand-scored complexity, or fixed task class
chooses the route. The selected route, rationale, exact deliberative input
count, and ceiling enter the canonical choice trace.

Later observed consequence or qualitative owner feedback produces bounded
`compute_calibration` evidence which subsequent choices consume contextually.
This is a PonderNet/CTM-style learned-halting seam over the frozen model, not a
claim that an adaptive-compute policy has already been trained or improved.
The live service loaded the change at the unchanged ordinal-26 state and
remained quiescent; no synthetic autobiographical event was injected merely to
exercise it. Focused wiring remains 39 PASS.

## 2026-09-02 sparse learned capability workspace

The flat recent capability dump is replaced on the active path by a bounded,
revision-aware module view. The frozen learned controller (and learned human-
turn router) chooses the applicable module keys from current state; an empty
selection is valid. Only selected module summaries reach the cortex, and later
objective consequence is recorded against exactly those selected keys. Older
revisions remain durable failure/revision evidence, while the active view
tracks the most recently revised module without imposing a permanent fixed
architecture width. This is a MoCL/SEMA/RIMs-style sparse activation seam, not
evidence that module selection has already improved general performance.

One assembled supervisor test proves active-revision selection, cortex
delivery, exact capability-use attribution, and consequence attachment. All
seven focused runtime files pass: 45 tests in 4.56 seconds. The live API
reloaded healthy at ordinal 26 with no pending operation and external effects
disabled; the reload did not create a cognitive episode.

## 2026-09-02 model-authored capability disposition and exact lineage

The production consolidation pass now uses the observed multidimensional
consequence to decide whether reusable capability retention is justified. A
retained proposal either targets an exact capability revision selected for the
attempt or requests a new opaque content-addressed identity; code performs no
semantic task matching and applies no reward threshold. A revision can change
its procedure text while preserving its durable key and exact supersession
link. A model-authored decline creates no capability record. Providers lacking
the disposition contract cannot silently promote their procedure.

Capability-use records now bind key, revision index, and exact capability hash.
The active module view exposes only use evidence for that exact revision,
separating older unversioned evidence explicitly. Content hashes and visible
revision continuity are validated before a module becomes active. Unevaluated
human and scheduler attempts append a deferred-disposition record but receive
no outcome or capability credit.

`SupervisorCanonicalMemory` now streams every bounded 256-row page and keeps a
bounded top-k fallback working set, fixing the long-life failure where Cognee
references newer than the first 256 episodes could not be canonically rejoined.
An assembled 300-episode test reaches the final page. Create, exact revision,
decline, deferred unevaluated handling, sparse delivery, and local consequence
attribution run through the complete supervisor transaction. Seven focused
runtime files: 49 PASS in 4.65 seconds. The live service reloaded healthy at
the unchanged ordinal-26 state without an episode. Its ordinary model output
still has no objective evaluator and therefore remains uncredited; this is
deliberate epistemic honesty, not a claim of live autonomous skill growth.

The apparent `256` ceiling is now explicitly only the canonical store's page
size. Pagination covers the full disk-backed history. The hot state keeps one
active head for every distinct capability rather than the latest 256 records,
while prior revisions remain in immutable canonical updates/episodes through
their exact supersession hashes. No default 64-module cutoff is applied to the
active catalog. A 300-capability assembled transaction selected capability 0,
completed, and retained all 300; the focused suite is 50 PASS in 4.79 seconds.
When the active catalog eventually approaches the model context/resource
boundary, the next scaling mechanism is Cognee-indexed sparse retrieval, not
silent deletion.

## 2026-09-02 owner-present native OpenClaw tool loop

The live API now has a separate same-owner `0600` bridge credential and an
exact native tool-turn protocol. On a human-initiated authenticated turn, the
learned controller may select one aggregate OpenClaw affordance; only then does
the same Qwen3.8 cortex receive the host's exact hash-bound dynamic catalog and
may author one or more native function calls. OpenClaw remains the guarded
executor. The autonomous scheduler cannot see this affordance, the workstation
does not convert host guarding into local authority, and intermediate results
advance neither the canonical episode nor Moving Origin. One final model answer
produces one receipt, learned update, episode, state-head commit, and projection.

Continuation state is durable and CAS-versioned, a multi-call result batch is
atomic, exact transport retries resume rather than duplicate a call or commit,
and error/denial status is preserved in the model-visible result. Five
experimental persisted record contracts are registered explicitly as
non-equivalent to approved Action, Feedback, or Episode contracts. Both new
SQLite stores use private race-safe files and validate their WAL/SHM sidecars.
Focused validation passed 61 tests plus 8 subtests in 7.89 seconds. A direct
bounded Qwen3.8 parser check requested the supplied `system_status` function
and then formed a final answer from its observed result. The live API restarted
at the unchanged ordinal 46 and unchanged state head
`sha256:15417090b1d074023592e8c2eebc06b4f0b4ac8f92c9a94812600e7a0cd6c07d`,
with no pending operation or projection. The remaining integration action is
one real harmless read-only OpenClaw handshake from the Windows host; no
synthetic catalog was pinned into Jenny's live state.

One fresh assembled rehearsal then exercised HTTP authentication, learned
aggregate selection, native Qwen tool authorship, an actually observed
read-only model-server result, continuation, final response, canonical commit,
Cognee projection, and exact replay. It returned `TOOL_REQUESTED` for
`system_status`, committed episode
`sha256:d247efce84fb0e6f6f682a24083a2f8d97a02aeb5e150110925889934870d650`,
and replayed that same episode in 0.0125 seconds without advancing ordinal 0.
Evidence:
`/opt/angler/results/jenny2/openclaw-fullpath-rehearsal-v1.json`, SHA-256
`ea97c159e8fb8a1d8755c78a3d4fd55c78ab36e47372b32dfd27ed6959412f59`.
The rehearsal used a separate state root and did not pin its synthetic catalog
or episode into the live identity.

## 2026-09-02 situated web observation and temporal boundary

The active learned controller can now choose between the cortex, federated
read-only search, and bounded public-HTTPS page reading from current
WORLD/SELF/FOCUS, temporal state, memory, and prior outcomes. Code supplies no
topic or curiosity table; it only canonicalizes model-authored tool objects,
enforces public-address/size/content-type boundaries, and binds observations to
source hashes. Search and a 12,000-character page read both passed against live
public endpoints. OpenClaw is absent from the active runtime as owner-directed.

The first autonomous web attempt is preserved as negative evidence: the model
selected `tool.web_research`, but encoded its valid payload as an object rather
than the contract's canonical string, so it failed before commit. The adapter
now performs that representation-only conversion and resolves valid named
last-outcome evidence aliases; 79 focused tests plus 3 subtests pass. A service
reload preserved state head
`sha256:56a1ac89f21c732366d9e0da211c583d36607d92fe885bcd9f4ab595847eb2d2`
and ordinal 52 exactly, with no pending work. The post-fix autonomous rerun is
deferred while the owner uses the live UI.

Temporal mechanics are implemented and transactionally tested, but semantic
temporal competence remains untrained and unqualified. The current 27B model
receives trusted UTC/local/monotonic time, Moving Origin order, and distinct
event/acquired/recorded/verified/valid fields. Relative-time interpretation is
still prompt-directed; no temporal adapter is loaded. Evidence:
`/opt/angler/results/jenny2/situated-web-temporal-boundary-v1.json`.

## 2026-09-03 initial LoRA activation and current-turn grounding

The mixed capability/temporal QLoRA completed all 1,024 steps. Its sealed
teacher-forced holdout reached loss `0.0005777` and token accuracy `0.9998248`;
base and adapted non-temporal retention controls both remained `1.0`. These are
curriculum results, not broad reasoning evidence. The content-addressed adapter
is served as
`jenny-qwen3.8-27b--lora-sha256-22a4e761a60822af0be15a84cb367472b95414a9df8d6add9334a193ecc8337d`.

The first live activation turn exposed and preserved a history-conditioning
failure as episode
`sha256:9f50234db0b69cfef7709283f061c0c1442cda242ba6e02bda7e1c2d917543e0`:
an unqualified adaptive-router fast path copied a prior web-search answer into
the new receipt. Observation, time, and Cognee recall were intact. The router
now must bind the current observation, treats retrieved capabilities as
candidates rather than automatically applicable modules, and retains its
output only as an auditable draft. The separately trained public cortex authors
the final response from the original request using the adapter's training-
matched non-thinking template.

An exact clone of the live canonical state and Cognee stores, including the bad
episode, then passed two sequential whole-cycle turns: it supplied correct
trusted local time/capability/uncertainty and next answered the unrelated query
with `The blue cup is beside the book.` Both episodes committed and projected;
restart preserved state
`sha256:27404a61dcb89c1acb973bcea28e0353aab4e15a61028d1255300c7074325a36`
with no pending operation or projection. Focused validation is 56 PASS. The
live service is enabled and healthy on the trained adapter at port 8088 with
the scheduler running and external effects disabled. Evidence:
`/opt/angler/results/jenny2/qwen38-lora-initial-training-live-activation-v1.json`,
SHA-256 `21114dfc35027e310bc5c106c81b236580a618428d2e20f78a34f62035e418a7`.

## 2026-09-03 candidate-only compact reading subgate

Status: `PASS`; exact candidate activated as a reversible local experimental
binding.

ADR `ANG-ADR-0011` adds a LOW-impact, effectless subgate for the immediate
functional question without weakening or replacing the full comparative
cumulative-binding qualification. Its fresh single-use identity is
`jenny2-candidate-reading-verification-r1`, and it emits
`ANG-CTR-JENNY-CANDIDATE-READING-VERIFICATION-001@0.1.0` using schema
`jenny2.candidate-only-whole-system-reading-verification.v1`.

The source/removal fixture and complete conjunctive gate were frozen before
any run. No R7 model output was inspected to choose the fixture, answer, or
disposition. Each arm receives one exact 188-byte Library work from
`experiments/corpora/jenny2-candidate-reading-verification-v1/`; their work
hashes are respectively
`b41fcd85f35b93b1676a10cdaa670f3abd4de092bf1bd5eb3a42730e9aa676c4`
and
`4fc33f973c0fd1c33499674666a1f6a313ecba78c9a12e576d4ce8c7199a653b`.

The run is exactly four high-level turns: read then answer on the source arm,
and the same read then the same answer request on the removal arm. Each turn
has one evaluator attempt and zero evaluator reruns or replacements. The
complete verification has a hard 600-second wall deadline. Internal component
calls remain subject to their existing bounds but are not assigned or reported
as an exact aggregate call count by this subgate.

The shared read and answer prompt SHA-256 values are respectively
`180b85ab1c7fe60a059e97bc7d8957e457a1eab18869f514b859ba14f0bdab92`
and `121832c0c648f163105e3b9abb20f068d018012a54b6002e22a14a2aff4aea65`;
ADR `ANG-ADR-0011` freezes their exact text and all budget fields.

The subgate passes only if both exact Library reads commit; close/reopen after
each read restores the exact canonical state ref, state bytes, Moving Origin
ordinal, and last-event ref with no pending work or projection; the source arm
returns exactly `{"answer":"VELLUM-684271","answer_available":true}`;
the removal arm returns exactly
`{"answer":null,"answer_available":false}`; Cognee canonically rejoins the
source-arm read into its answer context; bindings, calibration, sources, and
starting state remain unchanged; no scalar/capability credit changes; and
external effects remain disabled. A behavioral miss is `REJECT`; an
infrastructure, identity, integrity, restart, tamper, or accounting failure is
`INVALID`. Evidence is retained under either disposition and the identity is
never reused.

The existing `comparative-qualified-v2` activation basis remains the default.
Only a compact `PASS` plus the exact accepted offline calibration may use
`candidate-only-experimental-v1`, and then only as a reversible local
experimental binding with the prior binding preserved for rollback. The
record's comparative-evaluation, improvement, promotion, and readiness claim
flags are all false. It establishes neither broad reading nor long-document
retention, scientific promotion, system readiness, autonomy, identity,
personhood, feeling, consciousness, or AGI.

The single-use run completed in `353.906716` seconds and passed every frozen
conjunct. The source arm returned exactly
`{"answer":"VELLUM-684271","answer_available":true}` after its committed
read; the answer-lesioned arm returned exactly
`{"answer":null,"answer_available":false}`. Both arms restored the exact
post-read state across close/reopen and canonically rejoined their read episode
through Cognee for the answer turn. Each clone advanced from ordinal `62` to
`64` through its two committed turns; the read checkpoint itself survived
restart exactly, while the live/source input state root, fixture trees,
candidate binding, and calibration remained unchanged. No credit changed; no
work or projection was left pending; external effects remained disabled.
Evidence:
`/opt/angler/results/jenny2/candidate-reading-verification-r1/ba9c26e850bbdae7e0b331e641053f90f9dbfbf1b830937b2d11ff33059299d0.json`,
SHA-256
`ba9c26e850bbdae7e0b331e641053f90f9dbfbf1b830937b2d11ff33059299d0`.

The exact rank-16, weight-`0.375` candidate binding
`sha256:af7cfa72907fbcf4a8dd8db0c9510a1d3e395cd0231a5dd3281983b93f1b1bcc`
is now the live port-8088 service binding. Activation plan SHA-256:
`066d648529e65e378d2184155e82920092222ad026251489fbbb5912658fd74c`.
The prior rank-8 binding
`sha256:837d0b6c057d03e37096c80e66a1831e32991f4dadfbb43b1870f2a8c2463ff6`
and its service override are retained as exact rollback. Post-activation
`/health` reported API/model ready, the life loop running, and external effects
disabled; authenticated status reported ordinal `62`, state head
`sha256:ccaeb5063c29fd69dfb27215f4a880b0ec84d2a84862cf037333ee86ca34b866`,
no pending operation, and zero pending projections.

## 2026-09-04 owner-priority life-loop correction

Status: `PASS`; scheduling defect corrected without semantic-state mutation.

A consecutive-turn trace isolated the slow follow-up to a scheduler inversion,
not prompt accumulation or duplicate generation. The first owner turn completed
in `11.341578` seconds. The immediately following turn completed in
`26.802600` seconds, of which `15.071729` seconds were spent waiting for an
autonomous wake that had acquired the single operation lock at its 300-second
deadline. That wake used the 27B model, returned `QUIESCENT`, and advanced no
episode, artifact, or Moving Origin state.

Authenticated foreground activity now restarts a full idle interval before and
after `/v1/chat`, `/v1/chat/continue`, and `/v1/feedback`. A heartbeat only
attempts the operation lock nonblockingly; a busy owner path is never queued
behind it, and a skipped attempt does not consume autonomous step budget. No
answer, topic, greeting, story, emotion, or solution rule was added. The
learned initiative, Cognee, Moving Origin, affordances, and one-operation
transaction remain unchanged.

The focused runtime suite passed `111/111`; `git diff --check` passed. Service
restart preserved state
`sha256:f5f146d94fae97d539776d4040b034fc05998a79571193e7227281921f16dd5e`,
ordinal `76`, and last event
`sha256:8d94e6185bbaa052865d6f1600a7d67aa4d1b810b0e03338941d27151e67e35b`
with no pending operation or projection. Evidence:
`/opt/angler/results/jenny2/human-priority-life-loop-v1/result.json`, SHA-256
`41deecad6b787521f885f7797ecf848dad3fb4290501025c256d11b01d533b11`.

This corrects foreground responsiveness only. It does not claim that an empty
life loop has become useful; the next bounded task is an honest, model-authored
interest and artifact-production path with attributable durable outputs.

## 2026-09-04 authored-life continuation scope

Status: `active`; implementation is bounded by this section before its first
code write.

The next functional continuation restores the neutral behavior old Jenny
demonstrated—select work, produce a durable result, remember it, and later
revisit it—without copying recovered code or importing its deterministic
drives, mood rules, missions, consultant, random activity mix, topics, personal
state, or prompts. The recovered tree has no project-level license grant and is
behavioral evidence only.

Exact additional implementation scope is
`src/angler/runtime/authored_artifact.py`,
`src/angler/runtime/persistent_autonomy.py`,
`src/angler/runtime/higher_level_autonomy_adapter.py`,
`src/angler/runtime/frozen_cognitive_models.py`,
`src/angler/runtime/jenny2_runtime.py`, `scripts/jenny2_chat.py`, their listed
focused tests, `scripts/run_tool_blind_qualification_v2.py`,
`tests/unit/runtime/test_tool_blind_qualification_v2.py`, this leaf, bounded evidence under
`/opt/angler/results/jenny2/`, and append-only `AGENTS_SYNC.md`.

The scheduler may only detect a new canonical event, enforce idle time,
deduplicate a considered state, apply a resource ceiling, and yield to the
owner. It may not invent work. An unchanged dormant state must cause zero
model, recall, or semantic-state work. The frozen learned/model boundary may
independently choose dormancy or author one bounded request from visible state,
memory, time, unresolved evidence, and the dynamic affordance catalog.

One new internal affordance accepts a strict model-authored artifact intent and
returns a content-addressed observation. Its kind and subject are bounded free
text rather than a fixed cognitive menu; its content, purpose, evidence,
source-episode references, visibility, and predecessor are model-authored.
Trusted code validates size, canonical form, reference syntax, privacy, and
lineage only. The adapter appends an immutable artifact record to canonical
state so the normal episode/outbox transaction, Cognee projection, and Moving
Origin commit it exactly once. No filesystem or network write is added; the
existing separately allowlisted public read-only research tools are unchanged.

Acceptance requires zero-call repeated empty wakes; exactly one initiative
consideration after a new state; restart-safe deduplication; arbitrary learned
proposal changes changing activity/content; strict artifact bounds and
content identity; failed evidence/lineage checks producing no artifact;
immutable continuation; owner priority; one episode/origin advance and one
Cognee projection for a successful artifact; visibility without private
scratch or hidden chain-of-thought; focused tests; and a sealed cloned-state
production-model witness before live activation. Disabling registration must
stop new artifacts while preserving prior canonical history.
