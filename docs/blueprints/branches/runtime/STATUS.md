# RUNTIME status

Freshness: 2026-08-25  
Revision: 2  
Design: draft  
Delivery: not_started  
Current gate: `ANG-GATE-RUNTIME-DESIGN-001`

Completed: Tier-1 responsibility, state lifecycle, human-impact authorization boundary, child map, and conceptual contracts drafted.  
Blocker: model signature, LoRA topology, authorization enforcement, serialization, intervention, and compatibility details are unapproved.  
Next: expand MODEL-BOUNDARY, PLASTIC-STATE, and RUNTIME-COMPATIBILITY as one substrate package.  
Evidence: none.  
Rollback: remove branch package; Tier-0 blueprint remains unchanged.

## 2026-09-02 active integration successor

The owner explicitly approves the bounded higher-level composition in
`ANG-WORK-RUNTIME-HIGHER-LEVEL-EXPERIENCE-CYCLE-V1-001`. It joins the qualified
14B cortex, SEAM-style 1.7B experience generation, MemRL utility memory, ERL
reflection/consolidation, Agent-Lightning-style traces, Cognee, Moving Origin,
and Angler's valid consequence/integrity contracts in one shared cycle. The
first result is a local technical smoke, not a capability or readiness claim.
## 2026-09-02 higher-level assembled cycle

`angler.higher-level-experience-cycle.v1-r7-smoke` is technical PASS. The
92.144 s live run joined Qwen3-14B-NVFP4, frozen Qwen3-1.7B structured
experience/reflection/consolidation, canonical Cognee acquisition/search/writeback,
Moving Origin, semantic-plus-utility memory, objective feedback, refinement,
and an append-only 14-span trace. Both receipts passed the exact task verifier;
settled free VRAM was 3,322 MiB on GPU 0 and 7,286 MiB on GPU 1. This is one
composition witness, not improvement/readiness evidence. Next: whole-path
multi-domain continuity and removal controls.

## 2026-09-02 Jenny 2.0 dense functional baseline

Jenny 2.0 now uses the pinned dense Qwen3.8-27B-NVFP4 cortex through a
loopback-only SGLang TP2 runtime on two 295 W RTX 5080s. The supported
`language_model_only` configuration omits the visual tower. The service exposes
65,536 tokens and passed exact retrieval at 48,039 prompt tokens with 1,721 MiB
free per GPU afterward. ACT/ASK/WAIT state contrasts passed; one complete
effectless cycle committed and restarted exactly; Cognee recalled a prior
conversation across processes with unevaluated output labelled as such. The
focused runtime suite is 34 PASS. External effects remain disabled. Next:
owner-facing UI connection, then temporal-LoRA feasibility and learned
life-autonomy/capability training with causal retention controls.

## 2026-09-02 online procedure learning and bounded deliberation

The canonical Qwen3.8/Cognee/Moving-Origin cycle now consolidates explicit
procedure, applicability, and limits fields and conditions future choices on
multidimensional consequence history plus learning progress. Under fresh R3,
Jenny learned one arbitrary LUMEN-STEP procedure, restarted at the exact state
head, retrieved the learned procedure through Cognee, transferred it to a new
input, and lost the capability under a clean memory-removal control. Private
model deliberation remained enabled with native low effort and a 2,048-token
boundary. Result SHA-256:
`7938ee340038f8ae10eacf47c2cd3a78ef6826c226df798fe93489935b7436ac`.
This is a causal synthetic skill witness, not broad autonomous learning,
personhood, consciousness, or system readiness. Next is sequential multi-skill
retention and interference testing through the same live path.

## 2026-09-02 multi-skill retention and capability metacognition

Sequential multi-skill R2 learned three synthetic procedures, restarted, and
transferred all three exactly in reverse order with a clean removal control;
result SHA-256:
`c1ade4960da16850550f480f25b79e598d87a5727ece838eda0f1c5418c6cc70`.
The canonical state now carries bounded receipt-grounded capability evidence
into both controller and cortex. Capability-metacognition V1 preserved two
exact records across restart, accurately described applicability, limits, and
the difference between observed success and broad competence, then reported no
capabilities under clean state/memory removal. Result SHA-256:
`de2a55e7ebb7adf20441651a8ea80f1f11dd0e9b60186e55be0e4bf7d53496d2`.
This is functional metacognition, not evidence of subjective self-awareness or
consciousness. Next is bounded userless capability practice and revision.

## 2026-09-02 persistent owner API and active life loop

Jenny's current Qwen3.8/Cognee/Moving-Origin runtime is available through the
token-authenticated LAN API at `192.168.137.5:8088`. The enabled
`jenny2-api.service` waits for the loopback-only SGLang model, starts the
bounded heartbeat, serializes heartbeat and chat against one canonical state
head, and restarts on failure. Docker restarts the model container
`unless-stopped`; port 30000 remains loopback-only. A controlled service restart
restored the same state, token, address, and life-loop health. `/health` reports
ready only while model, scheduler, and heartbeat are all available. External
effects remain disabled; no external-effect affordances are registered.

## 2026-09-02 open-operation and qualitative education boundary

The live Qwen3.8 path now derives a variable set of context-specific operations
and compares them with reasons, predicted consequences, unknowns, and
reversibility. Fixed ASK/WAIT/STOP choices are absent from its cognitive
affordance set; those labels remain only in legacy/control compatibility code.
No pending learned initiative yields event-driven quiescence without advancing
Moving Origin. Model-produced evidence handles resolve to exact canonical
hashes, and qualitative owner correction can be recorded without a scalar or
global-utility update. Focused wiring: 36 PASS. Live API and Cognee projection
are healthy at Moving-Origin ordinal 22; external effects remain disabled.

## 2026-09-02 adaptive compute and human-readable temporal state

Metacognitive deliberation now scales with assembled input and reserves space
for its structured result; ceiling exhaustion invokes concise regeneration.
The receipt parked at ordinal 24 resumed exactly and committed at ordinal 25
without duplicate action or self-credit. The API now presents trusted UTC and
local date-time/timezone plus clock confidence as the Moving Origin, retaining
the ordinal only for exact transaction order. Live health is ready with no
pending operation or Cognee projection.

The input-matched controller ceiling now uses the serving tokenizer's exact
count. Model preference order/candidate payload is the single decision source;
inert JSON extras no longer crash execution, but all used capability/evidence
fields remain validated. The live cycle committed ordinal 26, preserved an
external evidence gap, and learned/model-authored an empty continuation. A
subsequent heartbeat returned `QUIESCENT` without advancing Moving Origin.

The current runtime now performs model-controlled adaptive compute: a compact
27B judgment can halt directly or request input-matched private deliberation.
The route and later objective/qualitative consequences are retained as bounded
contextual calibration, not a global hand-written compute rule. It is loaded
live at the unchanged ordinal-26 state; focused wiring is 39 PASS.

## 2026-09-02 sparse capability workspace

Capability history now forms revision-aware active modules without deleting
the canonical revision chain. The learned controller/router selects only the
context-relevant capability keys; only those module summaries enter the public
cortex workspace, and only selected modules receive attributable observed-
consequence evidence. No hand-written task router or global capability score
was added. The assembled focused runtime suite is 45 PASS. The live service
reloaded healthy at the unchanged ordinal-26 state with no pending operation;
no synthetic autobiographical episode was injected.

## 2026-09-02 consequence-sensitive capability lifecycle

Evaluated consolidation no longer automatically manufactures a skill or
guesses identity from procedure wording. The frozen model now proposes whether
to retain evidence as a new capability, revise an exact selected capability,
or retain no capability. New identities are content-addressed by trusted
transaction plumbing; revisions preserve the selected key and supersede the
exact revision hash actually used. Declines, unavailable disposition providers,
and unevaluated attempts remain explicit evidence without capability credit.
Capability history validates content hashes and visible supersession
continuity, and per-module outcome history is bound to exact revisions.

Canonical Cognee rejoin/fallback now pages through the complete episode store
instead of silently stopping at the earliest 256 records; bounded top-k recall
does not retain the whole candidate set in memory. The assembled focused suite
is 49 PASS. The live service is healthy at unchanged ordinal 26 with external
effects disabled. Ordinary live chat currently has no objective consequence
evaluator, so it remains honestly unevaluated and does not exercise capability
creation/revision; the new lifecycle is integration-qualified, not yet live-
learned evidence.

Capacity clarification: `256` is a bounded SQLite page size, not a database or
lifetime-memory ceiling. Every page is traversed. The active state retains one
head for every distinct capability with no fixed 64/256 catalog truncation;
superseded revisions remain immutable in prior canonical updates/episodes and
are linked by hash. Hot use evidence is compacted per capability only. An
assembled 300-capability cycle selected the oldest module and retained all 300.
Focused runtime suite: 50 PASS; live reload remained at ordinal 26.

## 2026-09-03 trained-adapter live boundary

The initial 1,024-step mixed capability/temporal LoRA completed and is loaded
through a content-addressed binding. A preserved live stale-history failure
showed that the adaptive effort router must not be final public-answer
authority. Current-turn evidence is now mandatory, retrieved capabilities are
only candidates, and every final answer is authored by the trained public
cortex. An exact live-state/Cognee clone containing that failure passed two
sequential whole-cycle turns and exact restart with no pending work; 56 focused
tests pass. The live port-8088 service is enabled and healthy on the adapter,
with the life loop running and external effects disabled. Evidence SHA-256:
`21114dfc35027e310bc5c106c81b236580a618428d2e20f78a34f62035e418a7`.

## 2026-09-03 qualified FAST_RESPONSE fusion

Ordinary qualified human turns now reuse the situated adaptive router's
complete public response instead of invoking the same active 27B public cortex
a second time. Situated recall, prediction, learned choice, reservation,
permission, receipt, reflection/consolidation, learned update, and atomic state
commit remain active; FULL/tool paths are unchanged. The optimization requires
an independent qualification ref bound to the exact active adapter/runtime;
missing qualification falls back to the original cortex and tampering fails
closed. A final isolated whole-system turn returned exact `FAST-GATE-OK` with
`choose_total=27.600210` seconds versus the prior 14-turn live mean of `43.814`
seconds. Focused validation is 72 PASS plus 3 subtests. The live service
restarted ready at unchanged ordinal 68 and unchanged state ref
`sha256:01174b235d9e4519cab35c34e9ee4370253891c2fb52f407135bb13fa2fa4b9f`.
Deployment evidence SHA-256:
`b2cec001aa06b4835ee6c869ffa5dc0d411729452231d163be182342a6bb6359`.
